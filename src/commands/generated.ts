// AUTO-GENERATED by scripts/generate-commands.mjs — DO NOT EDIT BY HAND.
// Covers every API operation not already hand-implemented in the other command
// files. Re-run `npm run gen` after an SDK bump. 256 commands.
import type { Argv } from 'yargs';
import { createClient } from '../client.js';
import { output } from '../utils/output.js';
import { handleError } from '../utils/errors.js';

export function registerGeneratedCommands(yargs: Argv): Argv {
  return yargs
    .command(
      'accounts:batch-get-google-business-reviews <accountId>',
      'Batch get reviews',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationNames', { type: 'string', describe: 'body param (comma-separated)', demandOption: true })
          .option('pageSize', { type: 'number', describe: 'body param' })
          .option('pageToken', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['locationNames'] !== undefined) body['locationNames'] = String(argv['locationNames']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['pageSize'] !== undefined) body['pageSize'] = argv['pageSize'];
          if (argv['pageToken'] !== undefined) body['pageToken'] = argv['pageToken'];
          const { data } = await (late as any).accounts.batchGetGoogleBusinessReviews({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accounts:delete-google-business-review-reply <accountId> <reviewId>',
      'Delete a review reply',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('reviewId', { type: 'string', describe: 'reviewId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], reviewId: argv['reviewId'] };
          const { data } = await (late as any).accounts.deleteGoogleBusinessReviewReply({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accounts:get-google-business-reviews <accountId>',
      'Get reviews',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('pageSize', { type: 'number', describe: 'query param' })
          .option('pageToken', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          if (argv['pageSize'] !== undefined) query['pageSize'] = argv['pageSize'];
          if (argv['pageToken'] !== undefined) query['pageToken'] = argv['pageToken'];
          const { data } = await (late as any).accounts.getGoogleBusinessReviews({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accounts:get-linked-in-mentions <accountId>',
      'Resolve LinkedIn mention',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('url', { type: 'string', describe: 'query param', demandOption: true })
          .option('displayName', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['url'] !== undefined) query['url'] = argv['url'];
          if (argv['displayName'] !== undefined) query['displayName'] = argv['displayName'];
          const { data } = await (late as any).accounts.getLinkedInMentions({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accounts:reply-to-google-business-review <accountId> <reviewId>',
      'Reply to a review',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('reviewId', { type: 'string', describe: 'reviewId', demandOption: true })
          .option('comment', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], reviewId: argv['reviewId'] };
          const body: Record<string, unknown> = {};
          if (argv['comment'] !== undefined) body['comment'] = argv['comment'];
          const { data } = await (late as any).accounts.replyToGoogleBusinessReview({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:delete-instagram-ice-breakers <accountId>',
      'Delete IG ice breakers',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).accountsettings.deleteInstagramIceBreakers({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:delete-messenger-menu <accountId>',
      'Delete FB persistent menu',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).accountsettings.deleteMessengerMenu({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:delete-telegram-commands <accountId>',
      'Delete TG bot commands',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).accountsettings.deleteTelegramCommands({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:get-instagram-ice-breakers <accountId>',
      'Get IG ice breakers',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).accountsettings.getInstagramIceBreakers({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:get-messenger-menu <accountId>',
      'Get FB persistent menu',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).accountsettings.getMessengerMenu({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:get-telegram-commands <accountId>',
      'Get TG bot commands',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).accountsettings.getTelegramCommands({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:set-instagram-ice-breakers <accountId>',
      'Set IG ice breakers',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('ice_breakers', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['ice_breakers'] !== undefined) body['ice_breakers'] = JSON.parse(String(argv['ice_breakers']));
          const { data } = await (late as any).accountsettings.setInstagramIceBreakers({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:set-messenger-menu <accountId>',
      'Set FB persistent menu',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('persistent_menu', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['persistent_menu'] !== undefined) body['persistent_menu'] = JSON.parse(String(argv['persistent_menu']));
          const { data } = await (late as any).accountsettings.setMessengerMenu({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'accountsettings:set-telegram-commands <accountId>',
      'Set TG bot commands',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('commands', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['commands'] !== undefined) body['commands'] = JSON.parse(String(argv['commands']));
          const { data } = await (late as any).accountsettings.setTelegramCommands({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adaudiences:add-users-to-ad-audience <audienceId>',
      'Add users to audience',
      (y) =>
        y
          .positional('audienceId', { type: 'string', describe: 'audienceId', demandOption: true })
          .option('users', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { audienceId: argv['audienceId'] };
          const body: Record<string, unknown> = {};
          if (argv['users'] !== undefined) body['users'] = JSON.parse(String(argv['users']));
          const { data } = await (late as any).adaudiences.addUsersToAdAudience({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adaudiences:create-ad-audience',
      'Create custom audience',
      (y) => y,
      async (argv) => {
        try {
          const late = createClient();

          const { data } = await (late as any).adaudiences.createAdAudience();
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adaudiences:delete-ad-audience <audienceId>',
      'Delete custom audience',
      (y) =>
        y
          .positional('audienceId', { type: 'string', describe: 'audienceId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { audienceId: argv['audienceId'] };
          const { data } = await (late as any).adaudiences.deleteAdAudience({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adaudiences:get-ad-audience <audienceId>',
      'Get audience details',
      (y) =>
        y
          .positional('audienceId', { type: 'string', describe: 'audienceId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { audienceId: argv['audienceId'] };
          const { data } = await (late as any).adaudiences.getAdAudience({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adaudiences:list-ad-audiences',
      'List custom audiences',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('type', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['type'] !== undefined) query['type'] = argv['type'];
          const { data } = await (late as any).adaudiences.listAdAudiences({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:bulk-update-ad-campaign-status',
      'Pause or resume many campaigns',
      (y) =>
        y
          .option('status', { type: 'string', describe: 'body param', demandOption: true })
          .option('campaigns', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['status'] !== undefined) body['status'] = argv['status'];
          if (argv['campaigns'] !== undefined) body['campaigns'] = JSON.parse(String(argv['campaigns']));
          const { data } = await (late as any).adcampaigns.bulkUpdateAdCampaignStatus({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:delete-ad-campaign <campaignId>',
      'Delete a campaign',
      (y) =>
        y
          .positional('campaignId', { type: 'string', describe: 'campaignId', demandOption: true })
          .option('platform', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { campaignId: argv['campaignId'] };
          const body: Record<string, unknown> = {};
          if (argv['platform'] !== undefined) body['platform'] = argv['platform'];
          const { data } = await (late as any).adcampaigns.deleteAdCampaign({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:duplicate-ad-campaign <campaignId>',
      'Duplicate a campaign',
      (y) =>
        y
          .positional('campaignId', { type: 'string', describe: 'campaignId', demandOption: true })
          .option('platform', { type: 'string', describe: 'body param', demandOption: true })
          .option('deepCopy', { type: 'boolean', describe: 'body param' })
          .option('statusOption', { type: 'string', describe: 'body param' })
          .option('startTime', { type: 'string', describe: 'body param' })
          .option('endTime', { type: 'string', describe: 'body param' })
          .option('renameStrategy', { type: 'string', describe: 'body param' })
          .option('renamePrefix', { type: 'string', describe: 'body param' })
          .option('renameSuffix', { type: 'string', describe: 'body param' })
          .option('syncAfter', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { campaignId: argv['campaignId'] };
          const body: Record<string, unknown> = {};
          if (argv['platform'] !== undefined) body['platform'] = argv['platform'];
          if (argv['deepCopy'] !== undefined) body['deepCopy'] = argv['deepCopy'];
          if (argv['statusOption'] !== undefined) body['statusOption'] = argv['statusOption'];
          if (argv['startTime'] !== undefined) body['startTime'] = argv['startTime'];
          if (argv['endTime'] !== undefined) body['endTime'] = argv['endTime'];
          if (argv['renameStrategy'] !== undefined) body['renameStrategy'] = argv['renameStrategy'];
          if (argv['renamePrefix'] !== undefined) body['renamePrefix'] = argv['renamePrefix'];
          if (argv['renameSuffix'] !== undefined) body['renameSuffix'] = argv['renameSuffix'];
          if (argv['syncAfter'] !== undefined) body['syncAfter'] = argv['syncAfter'];
          const { data } = await (late as any).adcampaigns.duplicateAdCampaign({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:get-ad-tree',
      'Get campaign tree',
      (y) =>
        y
          .option('page', { type: 'number', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('status', { type: 'string', describe: 'query param' })
          .option('adAccountId', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('campaignId', { type: 'string', describe: 'query param' })
          .option('fromDate', { type: 'string', describe: 'query param' })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('sort', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['page'] !== undefined) query['page'] = argv['page'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['campaignId'] !== undefined) query['campaignId'] = argv['campaignId'];
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['sort'] !== undefined) query['sort'] = argv['sort'];
          const { data } = await (late as any).adcampaigns.getAdTree({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:get-ads-timeline',
      'Get daily aggregate ad metrics for an account',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param' })
          .option('fromDate', { type: 'string', describe: 'query param' })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          const { data } = await (late as any).adcampaigns.getAdsTimeline({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:list-ad-campaigns',
      'List campaigns',
      (y) =>
        y
          .option('page', { type: 'number', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('status', { type: 'string', describe: 'query param' })
          .option('adAccountId', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('fromDate', { type: 'string', describe: 'query param' })
          .option('toDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['page'] !== undefined) query['page'] = argv['page'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          const { data } = await (late as any).adcampaigns.listAdCampaigns({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:update-ad-campaign <campaignId>',
      'Update a campaign (budget and/or bid strategy)',
      (y) =>
        y
          .positional('campaignId', { type: 'string', describe: 'campaignId', demandOption: true })
          .option('platform', { type: 'string', describe: 'body param', demandOption: true })
          .option('budget', { type: 'string', describe: 'body param (JSON)' })
          .option('bidStrategy', { type: 'string', describe: 'body param (JSON)' })
          .option('name', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { campaignId: argv['campaignId'] };
          const body: Record<string, unknown> = {};
          if (argv['platform'] !== undefined) body['platform'] = argv['platform'];
          if (argv['budget'] !== undefined) body['budget'] = JSON.parse(String(argv['budget']));
          if (argv['bidStrategy'] !== undefined) body['bidStrategy'] = JSON.parse(String(argv['bidStrategy']));
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          const { data } = await (late as any).adcampaigns.updateAdCampaign({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:update-ad-campaign-status <campaignId>',
      'Pause or resume a campaign',
      (y) =>
        y
          .positional('campaignId', { type: 'string', describe: 'campaignId', demandOption: true })
          .option('status', { type: 'string', describe: 'body param', demandOption: true })
          .option('platform', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { campaignId: argv['campaignId'] };
          const body: Record<string, unknown> = {};
          if (argv['status'] !== undefined) body['status'] = argv['status'];
          if (argv['platform'] !== undefined) body['platform'] = argv['platform'];
          const { data } = await (late as any).adcampaigns.updateAdCampaignStatus({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:update-ad-set <adSetId>',
      'Update an ad set (budget, status, and/or bid strategy)',
      (y) =>
        y
          .positional('adSetId', { type: 'string', describe: 'adSetId', demandOption: true })
          .option('platform', { type: 'string', describe: 'body param', demandOption: true })
          .option('budget', { type: 'string', describe: 'body param (JSON)' })
          .option('status', { type: 'string', describe: 'body param' })
          .option('name', { type: 'string', describe: 'body param' })
          .option('bidStrategy', { type: 'string', describe: 'body param (JSON)' })
          .option('bidAmount', { type: 'number', describe: 'body param' })
          .option('roasAverageFloor', { type: 'number', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adSetId: argv['adSetId'] };
          const body: Record<string, unknown> = {};
          if (argv['platform'] !== undefined) body['platform'] = argv['platform'];
          if (argv['budget'] !== undefined) body['budget'] = JSON.parse(String(argv['budget']));
          if (argv['status'] !== undefined) body['status'] = argv['status'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['bidStrategy'] !== undefined) body['bidStrategy'] = JSON.parse(String(argv['bidStrategy']));
          if (argv['bidAmount'] !== undefined) body['bidAmount'] = argv['bidAmount'];
          if (argv['roasAverageFloor'] !== undefined) body['roasAverageFloor'] = argv['roasAverageFloor'];
          const { data } = await (late as any).adcampaigns.updateAdSet({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'adcampaigns:update-ad-set-status <adSetId>',
      'Pause or resume a single ad set',
      (y) =>
        y
          .positional('adSetId', { type: 'string', describe: 'adSetId', demandOption: true })
          .option('status', { type: 'string', describe: 'body param', demandOption: true })
          .option('platform', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adSetId: argv['adSetId'] };
          const body: Record<string, unknown> = {};
          if (argv['status'] !== undefined) body['status'] = argv['status'];
          if (argv['platform'] !== undefined) body['platform'] = argv['platform'];
          const { data } = await (late as any).adcampaigns.updateAdSetStatus({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:add-conversion-associations <accountId> <destinationId>',
      'Associate campaigns with a conversion destination',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('destinationId', { type: 'string', describe: 'destinationId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('campaignIds', { type: 'string', describe: 'body param (comma-separated)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], destinationId: argv['destinationId'] };
          const body: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) body['adAccountId'] = argv['adAccountId'];
          if (argv['campaignIds'] !== undefined) body['campaignIds'] = String(argv['campaignIds']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).ads.addConversionAssociations({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:adjust-conversions',
      'Adjust already-uploaded conversions (Google only)',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('destinationId', { type: 'string', describe: 'body param', demandOption: true })
          .option('adjustments', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['destinationId'] !== undefined) body['destinationId'] = argv['destinationId'];
          if (argv['adjustments'] !== undefined) body['adjustments'] = JSON.parse(String(argv['adjustments']));
          const { data } = await (late as any).ads.adjustConversions({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:archive-lead-form <formId>',
      'Archive a Lead Gen form',
      (y) =>
        y
          .positional('formId', { type: 'string', describe: 'formId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { formId: argv['formId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).ads.archiveLeadForm({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:boost-post',
      'Boost post as ad',
      (y) =>
        y
          .option('postId', { type: 'string', describe: 'body param' })
          .option('platformPostId', { type: 'string', describe: 'body param' })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('goal', { type: 'string', describe: 'body param', demandOption: true })
          .option('budget', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('currency', { type: 'string', describe: 'body param' })
          .option('schedule', { type: 'string', describe: 'body param (JSON)' })
          .option('targeting', { type: 'string', describe: 'body param (JSON)' })
          .option('bidStrategy', { type: 'string', describe: 'body param (JSON)' })
          .option('bidAmount', { type: 'number', describe: 'body param' })
          .option('roasAverageFloor', { type: 'number', describe: 'body param' })
          .option('tracking', { type: 'string', describe: 'body param (JSON)' })
          .option('specialAdCategories', { type: 'string', describe: 'body param (comma-separated)' })
          .option('linkUrl', { type: 'string', describe: 'body param' })
          .option('callToAction', { type: 'string', describe: 'body param' })
          .option('sparkAuthCode', { type: 'string', describe: 'body param' })
          .option('dsaBeneficiary', { type: 'string', describe: 'body param' })
          .option('dsaPayor', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['postId'] !== undefined) body['postId'] = argv['postId'];
          if (argv['platformPostId'] !== undefined) body['platformPostId'] = argv['platformPostId'];
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) body['adAccountId'] = argv['adAccountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['goal'] !== undefined) body['goal'] = argv['goal'];
          if (argv['budget'] !== undefined) body['budget'] = JSON.parse(String(argv['budget']));
          if (argv['currency'] !== undefined) body['currency'] = argv['currency'];
          if (argv['schedule'] !== undefined) body['schedule'] = JSON.parse(String(argv['schedule']));
          if (argv['targeting'] !== undefined) body['targeting'] = JSON.parse(String(argv['targeting']));
          if (argv['bidStrategy'] !== undefined) body['bidStrategy'] = JSON.parse(String(argv['bidStrategy']));
          if (argv['bidAmount'] !== undefined) body['bidAmount'] = argv['bidAmount'];
          if (argv['roasAverageFloor'] !== undefined) body['roasAverageFloor'] = argv['roasAverageFloor'];
          if (argv['tracking'] !== undefined) body['tracking'] = JSON.parse(String(argv['tracking']));
          if (argv['specialAdCategories'] !== undefined) body['specialAdCategories'] = String(argv['specialAdCategories']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['linkUrl'] !== undefined) body['linkUrl'] = argv['linkUrl'];
          if (argv['callToAction'] !== undefined) body['callToAction'] = argv['callToAction'];
          if (argv['sparkAuthCode'] !== undefined) body['sparkAuthCode'] = argv['sparkAuthCode'];
          if (argv['dsaBeneficiary'] !== undefined) body['dsaBeneficiary'] = argv['dsaBeneficiary'];
          if (argv['dsaPayor'] !== undefined) body['dsaPayor'] = argv['dsaPayor'];
          const { data } = await (late as any).ads.boostPost({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:create-conversion-destination <accountId>',
      'Create a conversion destination (LinkedIn, Google Ads)',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('type', { type: 'string', describe: 'body param', demandOption: true })
          .option('attributionType', { type: 'string', describe: 'body param' })
          .option('postClickAttributionWindowSize', { type: 'number', describe: 'body param' })
          .option('viewThroughAttributionWindowSize', { type: 'number', describe: 'body param' })
          .option('valueType', { type: 'string', describe: 'body param' })
          .option('value', { type: 'string', describe: 'body param (JSON)' })
          .option('autoAssociationType', { type: 'string', describe: 'body param' })
          .option('countingType', { type: 'string', describe: 'body param' })
          .option('primaryForGoal', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) body['adAccountId'] = argv['adAccountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['type'] !== undefined) body['type'] = argv['type'];
          if (argv['attributionType'] !== undefined) body['attributionType'] = argv['attributionType'];
          if (argv['postClickAttributionWindowSize'] !== undefined) body['postClickAttributionWindowSize'] = argv['postClickAttributionWindowSize'];
          if (argv['viewThroughAttributionWindowSize'] !== undefined) body['viewThroughAttributionWindowSize'] = argv['viewThroughAttributionWindowSize'];
          if (argv['valueType'] !== undefined) body['valueType'] = argv['valueType'];
          if (argv['value'] !== undefined) body['value'] = JSON.parse(String(argv['value']));
          if (argv['autoAssociationType'] !== undefined) body['autoAssociationType'] = argv['autoAssociationType'];
          if (argv['countingType'] !== undefined) body['countingType'] = argv['countingType'];
          if (argv['primaryForGoal'] !== undefined) body['primaryForGoal'] = argv['primaryForGoal'];
          const { data } = await (late as any).ads.createConversionDestination({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:create-ctwa',
      'Create Click-to-WhatsApp ad(s)',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('headline', { type: 'string', describe: 'body param' })
          .option('body', { type: 'string', describe: 'body param' })
          .option('imageUrl', { type: 'string', describe: 'body param' })
          .option('video', { type: 'string', describe: 'body param (JSON)' })
          .option('creatives', { type: 'string', describe: 'body param (JSON)' })
          .option('budgetAmount', { type: 'number', describe: 'body param', demandOption: true })
          .option('budgetType', { type: 'string', describe: 'body param', demandOption: true })
          .option('currency', { type: 'string', describe: 'body param' })
          .option('endDate', { type: 'string', describe: 'body param' })
          .option('countries', { type: 'string', describe: 'body param (comma-separated)' })
          .option('cities', { type: 'string', describe: 'body param (JSON)' })
          .option('regions', { type: 'string', describe: 'body param (JSON)' })
          .option('zips', { type: 'string', describe: 'body param (JSON)' })
          .option('metros', { type: 'string', describe: 'body param (JSON)' })
          .option('customLocations', { type: 'string', describe: 'body param (JSON)' })
          .option('ageMin', { type: 'number', describe: 'body param' })
          .option('ageMax', { type: 'number', describe: 'body param' })
          .option('interests', { type: 'string', describe: 'body param (JSON)' })
          .option('audienceId', { type: 'string', describe: 'body param' })
          .option('advantageAudience', { type: 'number', describe: 'body param' })
          .option('objective', { type: 'string', describe: 'body param' })
          .option('bidStrategy', { type: 'string', describe: 'body param' })
          .option('bidAmount', { type: 'number', describe: 'body param' })
          .option('roasAverageFloor', { type: 'number', describe: 'body param' })
          .option('dsaBeneficiary', { type: 'string', describe: 'body param' })
          .option('dsaPayor', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) body['adAccountId'] = argv['adAccountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['headline'] !== undefined) body['headline'] = argv['headline'];
          if (argv['body'] !== undefined) body['body'] = argv['body'];
          if (argv['imageUrl'] !== undefined) body['imageUrl'] = argv['imageUrl'];
          if (argv['video'] !== undefined) body['video'] = JSON.parse(String(argv['video']));
          if (argv['creatives'] !== undefined) body['creatives'] = JSON.parse(String(argv['creatives']));
          if (argv['budgetAmount'] !== undefined) body['budgetAmount'] = argv['budgetAmount'];
          if (argv['budgetType'] !== undefined) body['budgetType'] = argv['budgetType'];
          if (argv['currency'] !== undefined) body['currency'] = argv['currency'];
          if (argv['endDate'] !== undefined) body['endDate'] = argv['endDate'];
          if (argv['countries'] !== undefined) body['countries'] = String(argv['countries']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['cities'] !== undefined) body['cities'] = JSON.parse(String(argv['cities']));
          if (argv['regions'] !== undefined) body['regions'] = JSON.parse(String(argv['regions']));
          if (argv['zips'] !== undefined) body['zips'] = JSON.parse(String(argv['zips']));
          if (argv['metros'] !== undefined) body['metros'] = JSON.parse(String(argv['metros']));
          if (argv['customLocations'] !== undefined) body['customLocations'] = JSON.parse(String(argv['customLocations']));
          if (argv['ageMin'] !== undefined) body['ageMin'] = argv['ageMin'];
          if (argv['ageMax'] !== undefined) body['ageMax'] = argv['ageMax'];
          if (argv['interests'] !== undefined) body['interests'] = JSON.parse(String(argv['interests']));
          if (argv['audienceId'] !== undefined) body['audienceId'] = argv['audienceId'];
          if (argv['advantageAudience'] !== undefined) body['advantageAudience'] = argv['advantageAudience'];
          if (argv['objective'] !== undefined) body['objective'] = argv['objective'];
          if (argv['bidStrategy'] !== undefined) body['bidStrategy'] = argv['bidStrategy'];
          if (argv['bidAmount'] !== undefined) body['bidAmount'] = argv['bidAmount'];
          if (argv['roasAverageFloor'] !== undefined) body['roasAverageFloor'] = argv['roasAverageFloor'];
          if (argv['dsaBeneficiary'] !== undefined) body['dsaBeneficiary'] = argv['dsaBeneficiary'];
          if (argv['dsaPayor'] !== undefined) body['dsaPayor'] = argv['dsaPayor'];
          const { data } = await (late as any).ads.createCtwaAd({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:create-lead-form',
      'Create a Lead Gen (Instant) form',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('questions', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('privacyPolicyUrl', { type: 'string', describe: 'body param', demandOption: true })
          .option('privacyPolicyLinkText', { type: 'string', describe: 'body param' })
          .option('followUpActionUrl', { type: 'string', describe: 'body param' })
          .option('locale', { type: 'string', describe: 'body param' })
          .option('thankYouTitle', { type: 'string', describe: 'body param' })
          .option('thankYouBody', { type: 'string', describe: 'body param' })
          .option('thankYouButtonText', { type: 'string', describe: 'body param' })
          .option('thankYouButtonType', { type: 'string', describe: 'body param' })
          .option('thankYouWebsiteUrl', { type: 'string', describe: 'body param' })
          .option('isOptimizedForQuality', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['questions'] !== undefined) body['questions'] = JSON.parse(String(argv['questions']));
          if (argv['privacyPolicyUrl'] !== undefined) body['privacyPolicyUrl'] = argv['privacyPolicyUrl'];
          if (argv['privacyPolicyLinkText'] !== undefined) body['privacyPolicyLinkText'] = argv['privacyPolicyLinkText'];
          if (argv['followUpActionUrl'] !== undefined) body['followUpActionUrl'] = argv['followUpActionUrl'];
          if (argv['locale'] !== undefined) body['locale'] = argv['locale'];
          if (argv['thankYouTitle'] !== undefined) body['thankYouTitle'] = argv['thankYouTitle'];
          if (argv['thankYouBody'] !== undefined) body['thankYouBody'] = argv['thankYouBody'];
          if (argv['thankYouButtonText'] !== undefined) body['thankYouButtonText'] = argv['thankYouButtonText'];
          if (argv['thankYouButtonType'] !== undefined) body['thankYouButtonType'] = argv['thankYouButtonType'];
          if (argv['thankYouWebsiteUrl'] !== undefined) body['thankYouWebsiteUrl'] = argv['thankYouWebsiteUrl'];
          if (argv['isOptimizedForQuality'] !== undefined) body['isOptimizedForQuality'] = argv['isOptimizedForQuality'];
          const { data } = await (late as any).ads.createLeadForm({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:create-standalone',
      'Create standalone ad',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('campaignName', { type: 'string', describe: 'body param' })
          .option('adSetName', { type: 'string', describe: 'body param' })
          .option('adName', { type: 'string', describe: 'body param' })
          .option('tracking', { type: 'string', describe: 'body param (JSON)' })
          .option('goal', { type: 'string', describe: 'body param' })
          .option('optimizationGoal', { type: 'string', describe: 'body param' })
          .option('budgetAmount', { type: 'number', describe: 'body param' })
          .option('budgetType', { type: 'string', describe: 'body param' })
          .option('budgetLevel', { type: 'string', describe: 'body param' })
          .option('currency', { type: 'string', describe: 'body param' })
          .option('headline', { type: 'string', describe: 'body param' })
          .option('longHeadline', { type: 'string', describe: 'body param' })
          .option('body', { type: 'string', describe: 'body param' })
          .option('description', { type: 'string', describe: 'body param' })
          .option('callToAction', { type: 'string', describe: 'body param' })
          .option('linkUrl', { type: 'string', describe: 'body param' })
          .option('leadGenFormId', { type: 'string', describe: 'body param' })
          .option('imageUrl', { type: 'string', describe: 'body param' })
          .option('images', { type: 'string', describe: 'body param (JSON)' })
          .option('video', { type: 'string', describe: 'body param (JSON)' })
          .option('creatives', { type: 'string', describe: 'body param (JSON)' })
          .option('adSetId', { type: 'string', describe: 'body param' })
          .option('businessName', { type: 'string', describe: 'body param' })
          .option('boardId', { type: 'string', describe: 'body param' })
          .option('organizationId', { type: 'string', describe: 'body param' })
          .option('countries', { type: 'string', describe: 'body param (comma-separated)' })
          .option('cities', { type: 'string', describe: 'body param (JSON)' })
          .option('regions', { type: 'string', describe: 'body param (JSON)' })
          .option('ageMin', { type: 'number', describe: 'body param' })
          .option('ageMax', { type: 'number', describe: 'body param' })
          .option('interests', { type: 'string', describe: 'body param (JSON)' })
          .option('zips', { type: 'string', describe: 'body param (JSON)' })
          .option('metros', { type: 'string', describe: 'body param (JSON)' })
          .option('customLocations', { type: 'string', describe: 'body param (JSON)' })
          .option('behaviors', { type: 'string', describe: 'body param (JSON)' })
          .option('incomeTier', { type: 'string', describe: 'body param' })
          .option('languages', { type: 'string', describe: 'body param (comma-separated)' })
          .option('placements', { type: 'string', describe: 'body param (JSON)' })
          .option('savedTargetingId', { type: 'string', describe: 'body param' })
          .option('rawTargeting', { type: 'string', describe: 'body param (JSON)' })
          .option('specialAdCategories', { type: 'string', describe: 'body param (comma-separated)' })
          .option('endDate', { type: 'string', describe: 'body param' })
          .option('startDate', { type: 'string', describe: 'body param' })
          .option('instagramAccountId', { type: 'string', describe: 'body param' })
          .option('dynamicCreative', { type: 'string', describe: 'body param (JSON)' })
          .option('placementAssets', { type: 'string', describe: 'body param (JSON)' })
          .option('audienceId', { type: 'string', describe: 'body param' })
          .option('campaignType', { type: 'string', describe: 'body param' })
          .option('keywords', { type: 'string', describe: 'body param (comma-separated)' })
          .option('additionalHeadlines', { type: 'string', describe: 'body param (comma-separated)' })
          .option('additionalDescriptions', { type: 'string', describe: 'body param (comma-separated)' })
          .option('advantageAudience', { type: 'number', describe: 'body param' })
          .option('attributionSpec', { type: 'string', describe: 'body param (JSON)' })
          .option('gender', { type: 'string', describe: 'body param' })
          .option('bidStrategy', { type: 'string', describe: 'body param (JSON)' })
          .option('bidAmount', { type: 'number', describe: 'body param' })
          .option('roasAverageFloor', { type: 'number', describe: 'body param' })
          .option('dsaBeneficiary', { type: 'string', describe: 'body param' })
          .option('dsaPayor', { type: 'string', describe: 'body param' })
          .option('brandIdentity', { type: 'string', describe: 'body param (JSON)' })
          .option('identityType', { type: 'string', describe: 'body param' })
          .option('promotedObject', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) body['adAccountId'] = argv['adAccountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['campaignName'] !== undefined) body['campaignName'] = argv['campaignName'];
          if (argv['adSetName'] !== undefined) body['adSetName'] = argv['adSetName'];
          if (argv['adName'] !== undefined) body['adName'] = argv['adName'];
          if (argv['tracking'] !== undefined) body['tracking'] = JSON.parse(String(argv['tracking']));
          if (argv['goal'] !== undefined) body['goal'] = argv['goal'];
          if (argv['optimizationGoal'] !== undefined) body['optimizationGoal'] = argv['optimizationGoal'];
          if (argv['budgetAmount'] !== undefined) body['budgetAmount'] = argv['budgetAmount'];
          if (argv['budgetType'] !== undefined) body['budgetType'] = argv['budgetType'];
          if (argv['budgetLevel'] !== undefined) body['budgetLevel'] = argv['budgetLevel'];
          if (argv['currency'] !== undefined) body['currency'] = argv['currency'];
          if (argv['headline'] !== undefined) body['headline'] = argv['headline'];
          if (argv['longHeadline'] !== undefined) body['longHeadline'] = argv['longHeadline'];
          if (argv['body'] !== undefined) body['body'] = argv['body'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['callToAction'] !== undefined) body['callToAction'] = argv['callToAction'];
          if (argv['linkUrl'] !== undefined) body['linkUrl'] = argv['linkUrl'];
          if (argv['leadGenFormId'] !== undefined) body['leadGenFormId'] = argv['leadGenFormId'];
          if (argv['imageUrl'] !== undefined) body['imageUrl'] = argv['imageUrl'];
          if (argv['images'] !== undefined) body['images'] = JSON.parse(String(argv['images']));
          if (argv['video'] !== undefined) body['video'] = JSON.parse(String(argv['video']));
          if (argv['creatives'] !== undefined) body['creatives'] = JSON.parse(String(argv['creatives']));
          if (argv['adSetId'] !== undefined) body['adSetId'] = argv['adSetId'];
          if (argv['businessName'] !== undefined) body['businessName'] = argv['businessName'];
          if (argv['boardId'] !== undefined) body['boardId'] = argv['boardId'];
          if (argv['organizationId'] !== undefined) body['organizationId'] = argv['organizationId'];
          if (argv['countries'] !== undefined) body['countries'] = String(argv['countries']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['cities'] !== undefined) body['cities'] = JSON.parse(String(argv['cities']));
          if (argv['regions'] !== undefined) body['regions'] = JSON.parse(String(argv['regions']));
          if (argv['ageMin'] !== undefined) body['ageMin'] = argv['ageMin'];
          if (argv['ageMax'] !== undefined) body['ageMax'] = argv['ageMax'];
          if (argv['interests'] !== undefined) body['interests'] = JSON.parse(String(argv['interests']));
          if (argv['zips'] !== undefined) body['zips'] = JSON.parse(String(argv['zips']));
          if (argv['metros'] !== undefined) body['metros'] = JSON.parse(String(argv['metros']));
          if (argv['customLocations'] !== undefined) body['customLocations'] = JSON.parse(String(argv['customLocations']));
          if (argv['behaviors'] !== undefined) body['behaviors'] = JSON.parse(String(argv['behaviors']));
          if (argv['incomeTier'] !== undefined) body['incomeTier'] = argv['incomeTier'];
          if (argv['languages'] !== undefined) body['languages'] = String(argv['languages']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['placements'] !== undefined) body['placements'] = JSON.parse(String(argv['placements']));
          if (argv['savedTargetingId'] !== undefined) body['savedTargetingId'] = argv['savedTargetingId'];
          if (argv['rawTargeting'] !== undefined) body['rawTargeting'] = JSON.parse(String(argv['rawTargeting']));
          if (argv['specialAdCategories'] !== undefined) body['specialAdCategories'] = String(argv['specialAdCategories']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['endDate'] !== undefined) body['endDate'] = argv['endDate'];
          if (argv['startDate'] !== undefined) body['startDate'] = argv['startDate'];
          if (argv['instagramAccountId'] !== undefined) body['instagramAccountId'] = argv['instagramAccountId'];
          if (argv['dynamicCreative'] !== undefined) body['dynamicCreative'] = JSON.parse(String(argv['dynamicCreative']));
          if (argv['placementAssets'] !== undefined) body['placementAssets'] = JSON.parse(String(argv['placementAssets']));
          if (argv['audienceId'] !== undefined) body['audienceId'] = argv['audienceId'];
          if (argv['campaignType'] !== undefined) body['campaignType'] = argv['campaignType'];
          if (argv['keywords'] !== undefined) body['keywords'] = String(argv['keywords']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['additionalHeadlines'] !== undefined) body['additionalHeadlines'] = String(argv['additionalHeadlines']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['additionalDescriptions'] !== undefined) body['additionalDescriptions'] = String(argv['additionalDescriptions']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['advantageAudience'] !== undefined) body['advantageAudience'] = argv['advantageAudience'];
          if (argv['attributionSpec'] !== undefined) body['attributionSpec'] = JSON.parse(String(argv['attributionSpec']));
          if (argv['gender'] !== undefined) body['gender'] = argv['gender'];
          if (argv['bidStrategy'] !== undefined) body['bidStrategy'] = JSON.parse(String(argv['bidStrategy']));
          if (argv['bidAmount'] !== undefined) body['bidAmount'] = argv['bidAmount'];
          if (argv['roasAverageFloor'] !== undefined) body['roasAverageFloor'] = argv['roasAverageFloor'];
          if (argv['dsaBeneficiary'] !== undefined) body['dsaBeneficiary'] = argv['dsaBeneficiary'];
          if (argv['dsaPayor'] !== undefined) body['dsaPayor'] = argv['dsaPayor'];
          if (argv['brandIdentity'] !== undefined) body['brandIdentity'] = JSON.parse(String(argv['brandIdentity']));
          if (argv['identityType'] !== undefined) body['identityType'] = argv['identityType'];
          if (argv['promotedObject'] !== undefined) body['promotedObject'] = JSON.parse(String(argv['promotedObject']));
          const { data } = await (late as any).ads.createStandaloneAd({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:create-test-lead <formId>',
      'Create a synthetic test lead',
      (y) =>
        y
          .positional('formId', { type: 'string', describe: 'formId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('fieldData', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { formId: argv['formId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['fieldData'] !== undefined) body['fieldData'] = JSON.parse(String(argv['fieldData']));
          const { data } = await (late as any).ads.createTestLead({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:delete <adId>',
      'Cancel an ad',
      (y) =>
        y
          .positional('adId', { type: 'string', describe: 'adId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adId: argv['adId'] };
          const { data } = await (late as any).ads.deleteAd({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:delete-conversion-destination <accountId> <destinationId>',
      'Soft-delete a conversion destination',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('destinationId', { type: 'string', describe: 'destinationId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], destinationId: argv['destinationId'] };
          const query: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          const { data } = await (late as any).ads.deleteConversionDestination({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:estimate-reach',
      'Estimate audience reach',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('spec', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('optimizationGoal', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) body['adAccountId'] = argv['adAccountId'];
          if (argv['spec'] !== undefined) body['spec'] = JSON.parse(String(argv['spec']));
          if (argv['optimizationGoal'] !== undefined) body['optimizationGoal'] = argv['optimizationGoal'];
          const { data } = await (late as any).ads.estimateAdReach({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get <adId>',
      'Get ad details',
      (y) =>
        y
          .positional('adId', { type: 'string', describe: 'adId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adId: argv['adId'] };
          const { data } = await (late as any).ads.getAd({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get-analytics <adId>',
      'Get ad analytics',
      (y) =>
        y
          .positional('adId', { type: 'string', describe: 'adId', demandOption: true })
          .option('fromDate', { type: 'string', describe: 'query param' })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('breakdowns', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adId: argv['adId'] };
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['breakdowns'] !== undefined) query['breakdowns'] = argv['breakdowns'];
          const { data } = await (late as any).ads.getAdAnalytics({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get-comments <adId>',
      'List comments on an ad',
      (y) =>
        y
          .positional('adId', { type: 'string', describe: 'adId', demandOption: true })
          .option('placement', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('cursor', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adId: argv['adId'] };
          const query: Record<string, unknown> = {};
          if (argv['placement'] !== undefined) query['placement'] = argv['placement'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['cursor'] !== undefined) query['cursor'] = argv['cursor'];
          const { data } = await (late as any).ads.getAdComments({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get-conversion-destination <accountId> <destinationId>',
      'Fetch a single conversion destination',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('destinationId', { type: 'string', describe: 'destinationId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], destinationId: argv['destinationId'] };
          const query: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          const { data } = await (late as any).ads.getConversionDestination({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get-conversion-metrics <accountId> <destinationId>',
      'Fetch attribution metrics for a conversion destination',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('destinationId', { type: 'string', describe: 'destinationId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('startDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('endDate', { type: 'string', describe: 'query param' })
          .option('granularity', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], destinationId: argv['destinationId'] };
          const query: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['startDate'] !== undefined) query['startDate'] = argv['startDate'];
          if (argv['endDate'] !== undefined) query['endDate'] = argv['endDate'];
          if (argv['granularity'] !== undefined) query['granularity'] = argv['granularity'];
          const { data } = await (late as any).ads.getConversionMetrics({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get-conversions-quality',
      'Read Event Match Quality + coverage for a Meta pixel',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('destinationId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['destinationId'] !== undefined) query['destinationId'] = argv['destinationId'];
          const { data } = await (late as any).ads.getConversionsQuality({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get-lead-form <formId>',
      'Get a single Lead Gen form',
      (y) =>
        y
          .positional('formId', { type: 'string', describe: 'formId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { formId: argv['formId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).ads.getLeadForm({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:get-tracking-tags <adId>',
      'Read an ad\'s click-URL tracking tags',
      (y) =>
        y
          .positional('adId', { type: 'string', describe: 'adId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adId: argv['adId'] };
          const { data } = await (late as any).ads.getAdTrackingTags({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list',
      'List ads',
      (y) =>
        y
          .option('page', { type: 'number', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' })
          .option('status', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('adAccountId', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('campaignId', { type: 'string', describe: 'query param' })
          .option('platformAdId', { type: 'string', describe: 'query param' })
          .option('effectiveObjectStoryId', { type: 'string', describe: 'query param' })
          .option('effectiveInstagramMediaId', { type: 'string', describe: 'query param' })
          .option('fromDate', { type: 'string', describe: 'query param' })
          .option('toDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['page'] !== undefined) query['page'] = argv['page'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['campaignId'] !== undefined) query['campaignId'] = argv['campaignId'];
          if (argv['platformAdId'] !== undefined) query['platformAdId'] = argv['platformAdId'];
          if (argv['effectiveObjectStoryId'] !== undefined) query['effectiveObjectStoryId'] = argv['effectiveObjectStoryId'];
          if (argv['effectiveInstagramMediaId'] !== undefined) query['effectiveInstagramMediaId'] = argv['effectiveInstagramMediaId'];
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          const { data } = await (late as any).ads.listAds({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-accounts',
      'List ad accounts',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          const { data } = await (late as any).ads.listAdAccounts({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-business-centers',
      'List TikTok Business Centers',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).ads.listAdsBusinessCenters({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-catalog-product-sets <catalogId>',
      'List a catalog\'s product sets',
      (y) =>
        y
          .positional('catalogId', { type: 'string', describe: 'catalogId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { catalogId: argv['catalogId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).ads.listAdCatalogProductSets({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-catalogs',
      'List Meta product catalogs',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          const { data } = await (late as any).ads.listAdCatalogs({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-conversion-associations <accountId> <destinationId>',
      'List campaigns associated with a conversion destination',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('destinationId', { type: 'string', describe: 'destinationId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], destinationId: argv['destinationId'] };
          const query: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          const { data } = await (late as any).ads.listConversionAssociations({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-conversion-destinations <accountId>',
      'List destinations for the Conversions API',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).ads.listConversionDestinations({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-form-leads <formId>',
      'List leads for a single form',
      (y) =>
        y
          .positional('formId', { type: 'string', describe: 'formId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('cursor', { type: 'string', describe: 'query param' })
          .option('since', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { formId: argv['formId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['cursor'] !== undefined) query['cursor'] = argv['cursor'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          const { data } = await (late as any).ads.listFormLeads({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-lead-forms',
      'List Lead Gen (Instant) forms',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('cursor', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['cursor'] !== undefined) query['cursor'] = argv['cursor'];
          const { data } = await (late as any).ads.listLeadForms({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:list-leads',
      'List submitted leads (cross-form CRM view)',
      (y) =>
        y
          .option('formId', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('since', { type: 'number', describe: 'query param' })
          .option('cursor', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['formId'] !== undefined) query['formId'] = argv['formId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['cursor'] !== undefined) query['cursor'] = argv['cursor'];
          const { data } = await (late as any).ads.listLeads({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:remove-conversion-associations <accountId> <destinationId>',
      'Remove campaign↔conversion associations',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('destinationId', { type: 'string', describe: 'destinationId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('campaignIds', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], destinationId: argv['destinationId'] };
          const query: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['campaignIds'] !== undefined) query['campaignIds'] = argv['campaignIds'];
          const { data } = await (late as any).ads.removeConversionAssociations({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:search-interests',
      'Search targeting interests (deprecated)',
      (y) =>
        y
          .option('q', { type: 'string', describe: 'query param', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['q'] !== undefined) query['q'] = argv['q'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).ads.searchAdInterests({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:search-targeting',
      'Search targeting options',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('q', { type: 'string', describe: 'query param', demandOption: true })
          .option('dimension', { type: 'string', describe: 'query param' })
          .option('geoType', { type: 'string', describe: 'query param' })
          .option('countryCode', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['q'] !== undefined) query['q'] = argv['q'];
          if (argv['dimension'] !== undefined) query['dimension'] = argv['dimension'];
          if (argv['geoType'] !== undefined) query['geoType'] = argv['geoType'];
          if (argv['countryCode'] !== undefined) query['countryCode'] = argv['countryCode'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          const { data } = await (late as any).ads.searchAdTargeting({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:send-conversions',
      'Send conversion events to an ad platform',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('destinationId', { type: 'string', describe: 'body param', demandOption: true })
          .option('events', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('testCode', { type: 'string', describe: 'body param' })
          .option('consent', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['destinationId'] !== undefined) body['destinationId'] = argv['destinationId'];
          if (argv['events'] !== undefined) body['events'] = JSON.parse(String(argv['events']));
          if (argv['testCode'] !== undefined) body['testCode'] = argv['testCode'];
          if (argv['consent'] !== undefined) body['consent'] = JSON.parse(String(argv['consent']));
          const { data } = await (late as any).ads.sendConversions({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:update <adId>',
      'Update ad',
      (y) =>
        y
          .positional('adId', { type: 'string', describe: 'adId', demandOption: true })
          .option('status', { type: 'string', describe: 'body param' })
          .option('budget', { type: 'string', describe: 'body param (JSON)' })
          .option('targeting', { type: 'string', describe: 'body param (JSON)' })
          .option('creative', { type: 'string', describe: 'body param (JSON)' })
          .option('name', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adId: argv['adId'] };
          const body: Record<string, unknown> = {};
          if (argv['status'] !== undefined) body['status'] = argv['status'];
          if (argv['budget'] !== undefined) body['budget'] = JSON.parse(String(argv['budget']));
          if (argv['targeting'] !== undefined) body['targeting'] = JSON.parse(String(argv['targeting']));
          if (argv['creative'] !== undefined) body['creative'] = JSON.parse(String(argv['creative']));
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          const { data } = await (late as any).ads.updateAd({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:update-conversion-destination <accountId> <destinationId>',
      'Update a conversion destination',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('destinationId', { type: 'string', describe: 'destinationId', demandOption: true })
          .option('adAccountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param' })
          .option('enabled', { type: 'boolean', describe: 'body param' })
          .option('attributionType', { type: 'string', describe: 'body param' })
          .option('postClickAttributionWindowSize', { type: 'number', describe: 'body param' })
          .option('viewThroughAttributionWindowSize', { type: 'number', describe: 'body param' })
          .option('valueType', { type: 'string', describe: 'body param' })
          .option('value', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], destinationId: argv['destinationId'] };
          const body: Record<string, unknown> = {};
          if (argv['adAccountId'] !== undefined) body['adAccountId'] = argv['adAccountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['enabled'] !== undefined) body['enabled'] = argv['enabled'];
          if (argv['attributionType'] !== undefined) body['attributionType'] = argv['attributionType'];
          if (argv['postClickAttributionWindowSize'] !== undefined) body['postClickAttributionWindowSize'] = argv['postClickAttributionWindowSize'];
          if (argv['viewThroughAttributionWindowSize'] !== undefined) body['viewThroughAttributionWindowSize'] = argv['viewThroughAttributionWindowSize'];
          if (argv['valueType'] !== undefined) body['valueType'] = argv['valueType'];
          if (argv['value'] !== undefined) body['value'] = JSON.parse(String(argv['value']));
          const { data } = await (late as any).ads.updateConversionDestination({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'ads:update-tracking-tags <adId>',
      'Set/update an ad\'s click-URL tracking tags',
      (y) =>
        y
          .positional('adId', { type: 'string', describe: 'adId', demandOption: true })
          .option('urlTags', { type: 'string', describe: 'body param (JSON)' })
          .option('creative', { type: 'string', describe: 'body param (JSON)' })
          .option('trackingUrlTemplate', { type: 'string', describe: 'body param' })
          .option('finalUrlSuffix', { type: 'string', describe: 'body param' })
          .option('dynamicValueParameters', { type: 'string', describe: 'body param (JSON)' })
          .option('customValueParameters', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { adId: argv['adId'] };
          const body: Record<string, unknown> = {};
          if (argv['urlTags'] !== undefined) body['urlTags'] = JSON.parse(String(argv['urlTags']));
          if (argv['creative'] !== undefined) body['creative'] = JSON.parse(String(argv['creative']));
          if (argv['trackingUrlTemplate'] !== undefined) body['trackingUrlTemplate'] = argv['trackingUrlTemplate'];
          if (argv['finalUrlSuffix'] !== undefined) body['finalUrlSuffix'] = argv['finalUrlSuffix'];
          if (argv['dynamicValueParameters'] !== undefined) body['dynamicValueParameters'] = JSON.parse(String(argv['dynamicValueParameters']));
          if (argv['customValueParameters'] !== undefined) body['customValueParameters'] = JSON.parse(String(argv['customValueParameters']));
          const { data } = await (late as any).ads.updateAdTrackingTags({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-content-decay',
      'Get content performance decay',
      (y) =>
        y
          .option('platform', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          const { data } = await (late as any).analytics.getContentDecay({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-facebook-page-insights',
      'Get Facebook Page insights',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metrics', { type: 'string', describe: 'query param' })
          .option('since', { type: 'string', describe: 'query param' })
          .option('until', { type: 'string', describe: 'query param' })
          .option('metricType', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['until'] !== undefined) query['until'] = argv['until'];
          if (argv['metricType'] !== undefined) query['metricType'] = argv['metricType'];
          const { data } = await (late as any).analytics.getFacebookPageInsights({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-google-business-performance',
      'Get GBP performance metrics',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metrics', { type: 'string', describe: 'query param' })
          .option('startDate', { type: 'string', describe: 'query param' })
          .option('endDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          if (argv['startDate'] !== undefined) query['startDate'] = argv['startDate'];
          if (argv['endDate'] !== undefined) query['endDate'] = argv['endDate'];
          const { data } = await (late as any).analytics.getGoogleBusinessPerformance({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-google-business-search-keywords',
      'Get GBP search keywords',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('startMonth', { type: 'string', describe: 'query param' })
          .option('endMonth', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['startMonth'] !== undefined) query['startMonth'] = argv['startMonth'];
          if (argv['endMonth'] !== undefined) query['endMonth'] = argv['endMonth'];
          const { data } = await (late as any).analytics.getGoogleBusinessSearchKeywords({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-instagram-account-insights',
      'Get Instagram insights',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metrics', { type: 'string', describe: 'query param' })
          .option('since', { type: 'string', describe: 'query param' })
          .option('until', { type: 'string', describe: 'query param' })
          .option('metricType', { type: 'string', describe: 'query param' })
          .option('breakdown', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['until'] !== undefined) query['until'] = argv['until'];
          if (argv['metricType'] !== undefined) query['metricType'] = argv['metricType'];
          if (argv['breakdown'] !== undefined) query['breakdown'] = argv['breakdown'];
          const { data } = await (late as any).analytics.getInstagramAccountInsights({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-instagram-demographics',
      'Get Instagram demographics',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metric', { type: 'string', describe: 'query param' })
          .option('breakdown', { type: 'string', describe: 'query param' })
          .option('timeframe', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metric'] !== undefined) query['metric'] = argv['metric'];
          if (argv['breakdown'] !== undefined) query['breakdown'] = argv['breakdown'];
          if (argv['timeframe'] !== undefined) query['timeframe'] = argv['timeframe'];
          const { data } = await (late as any).analytics.getInstagramDemographics({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-instagram-follower-history',
      'Get Instagram follower history',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metrics', { type: 'string', describe: 'query param' })
          .option('since', { type: 'string', describe: 'query param' })
          .option('until', { type: 'string', describe: 'query param' })
          .option('metricType', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['until'] !== undefined) query['until'] = argv['until'];
          if (argv['metricType'] !== undefined) query['metricType'] = argv['metricType'];
          const { data } = await (late as any).analytics.getInstagramFollowerHistory({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-linked-in-aggregate <accountId>',
      'Get LinkedIn aggregate stats',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('aggregation', { type: 'string', describe: 'query param' })
          .option('startDate', { type: 'string', describe: 'query param' })
          .option('endDate', { type: 'string', describe: 'query param' })
          .option('metrics', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['aggregation'] !== undefined) query['aggregation'] = argv['aggregation'];
          if (argv['startDate'] !== undefined) query['startDate'] = argv['startDate'];
          if (argv['endDate'] !== undefined) query['endDate'] = argv['endDate'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          const { data } = await (late as any).analytics.getLinkedInAggregateAnalytics({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-linked-in-org-aggregate',
      'Get LinkedIn organization page aggregate analytics',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metrics', { type: 'string', describe: 'query param' })
          .option('since', { type: 'string', describe: 'query param' })
          .option('until', { type: 'string', describe: 'query param' })
          .option('metricType', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['until'] !== undefined) query['until'] = argv['until'];
          if (argv['metricType'] !== undefined) query['metricType'] = argv['metricType'];
          const { data } = await (late as any).analytics.getLinkedInOrgAggregateAnalytics({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-linked-in-post <accountId>',
      'Get LinkedIn post stats',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('urn', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['urn'] !== undefined) query['urn'] = argv['urn'];
          const { data } = await (late as any).analytics.getLinkedInPostAnalytics({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-linked-in-post-reactions <accountId>',
      'Get LinkedIn post reactions',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('urn', { type: 'string', describe: 'query param', demandOption: true })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('cursor', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['urn'] !== undefined) query['urn'] = argv['urn'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['cursor'] !== undefined) query['cursor'] = argv['cursor'];
          const { data } = await (late as any).analytics.getLinkedInPostReactions({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-post-timeline',
      'Get post analytics timeline',
      (y) =>
        y
          .option('postId', { type: 'string', describe: 'query param', demandOption: true })
          .option('fromDate', { type: 'string', describe: 'query param' })
          .option('toDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['postId'] !== undefined) query['postId'] = argv['postId'];
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          const { data } = await (late as any).analytics.getPostTimeline({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-posting-frequency',
      'Get frequency vs engagement',
      (y) =>
        y
          .option('platform', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          const { data } = await (late as any).analytics.getPostingFrequency({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-tik-tok-account-insights',
      'Get TikTok account-level insights',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metrics', { type: 'string', describe: 'query param' })
          .option('since', { type: 'string', describe: 'query param' })
          .option('until', { type: 'string', describe: 'query param' })
          .option('metricType', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['until'] !== undefined) query['until'] = argv['until'];
          if (argv['metricType'] !== undefined) query['metricType'] = argv['metricType'];
          const { data } = await (late as any).analytics.getTikTokAccountInsights({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-you-tube-channel-insights',
      'Get YouTube channel-level insights',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('metrics', { type: 'string', describe: 'query param' })
          .option('since', { type: 'string', describe: 'query param' })
          .option('until', { type: 'string', describe: 'query param' })
          .option('metricType', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['metrics'] !== undefined) query['metrics'] = argv['metrics'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['until'] !== undefined) query['until'] = argv['until'];
          if (argv['metricType'] !== undefined) query['metricType'] = argv['metricType'];
          const { data } = await (late as any).analytics.getYouTubeChannelInsights({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-you-tube-daily-views',
      'Get YouTube daily views',
      (y) =>
        y
          .option('videoId', { type: 'string', describe: 'query param', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('startDate', { type: 'string', describe: 'query param' })
          .option('endDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['videoId'] !== undefined) query['videoId'] = argv['videoId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['startDate'] !== undefined) query['startDate'] = argv['startDate'];
          if (argv['endDate'] !== undefined) query['endDate'] = argv['endDate'];
          const { data } = await (late as any).analytics.getYouTubeDailyViews({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-you-tube-demographics',
      'Get YouTube demographics',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('breakdown', { type: 'string', describe: 'query param' })
          .option('startDate', { type: 'string', describe: 'query param' })
          .option('endDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['breakdown'] !== undefined) query['breakdown'] = argv['breakdown'];
          if (argv['startDate'] !== undefined) query['startDate'] = argv['startDate'];
          if (argv['endDate'] !== undefined) query['endDate'] = argv['endDate'];
          const { data } = await (late as any).analytics.getYouTubeDemographics({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'analytics:get-you-tube-video-retention',
      'Get YouTube video retention curve',
      (y) =>
        y
          .option('videoId', { type: 'string', describe: 'query param', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('startDate', { type: 'string', describe: 'query param' })
          .option('endDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['videoId'] !== undefined) query['videoId'] = argv['videoId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['startDate'] !== undefined) query['startDate'] = argv['startDate'];
          if (argv['endDate'] !== undefined) query['endDate'] = argv['endDate'];
          const { data } = await (late as any).analytics.getYouTubeVideoRetention({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:ads <platform>',
      'Connect ads for a platform',
      (y) =>
        y
          .positional('platform', { type: 'string', describe: 'platform', demandOption: true })
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('redirect_url', { type: 'string', describe: 'query param' })
          .option('headless', { type: 'boolean', describe: 'query param' })
          .option('adAccountId', { type: 'string', describe: 'query param' })
          .option('adAccountIds', { type: 'string', describe: 'query param (comma-separated)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { platform: argv['platform'] };
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['redirect_url'] !== undefined) query['redirect_url'] = argv['redirect_url'];
          if (argv['headless'] !== undefined) query['headless'] = argv['headless'];
          if (argv['adAccountId'] !== undefined) query['adAccountId'] = argv['adAccountId'];
          if (argv['adAccountIds'] !== undefined) query['adAccountIds'] = String(argv['adAccountIds']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).connect.connectAds({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:complete-whats-app-phone-selection',
      'Complete WhatsApp phone number selection',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true })
          .option('phoneNumberId', { type: 'string', describe: 'body param', demandOption: true })
          .option('wabaId', { type: 'string', describe: 'body param', demandOption: true })
          .option('tempToken', { type: 'string', describe: 'body param', demandOption: true })
          .option('userProfile', { type: 'string', describe: 'body param (JSON)' })
          .option('redirect_url', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          if (argv['phoneNumberId'] !== undefined) body['phoneNumberId'] = argv['phoneNumberId'];
          if (argv['wabaId'] !== undefined) body['wabaId'] = argv['wabaId'];
          if (argv['tempToken'] !== undefined) body['tempToken'] = argv['tempToken'];
          if (argv['userProfile'] !== undefined) body['userProfile'] = JSON.parse(String(argv['userProfile']));
          if (argv['redirect_url'] !== undefined) body['redirect_url'] = argv['redirect_url'];
          const { data } = await (late as any).connect.completeWhatsAppPhoneSelection({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:configure-tik-tok-ads-brand-identity',
      'Configure TikTok Ads Brand Identity',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('displayName', { type: 'string', describe: 'body param', demandOption: true })
          .option('imageUrl', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['displayName'] !== undefined) body['displayName'] = argv['displayName'];
          if (argv['imageUrl'] !== undefined) body['imageUrl'] = argv['imageUrl'];
          const { data } = await (late as any).connect.configureTikTokAdsBrandIdentity({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-facebook-pages <accountId>',
      'List Facebook pages',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('refresh', { type: 'boolean', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['refresh'] !== undefined) query['refresh'] = argv['refresh'];
          const { data } = await (late as any).connect.getFacebookPages({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-gmb-locations <accountId>',
      'List GBP locations',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('search', { type: 'string', describe: 'query param' })
          .option('filter', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['search'] !== undefined) query['search'] = argv['search'];
          if (argv['filter'] !== undefined) query['filter'] = argv['filter'];
          const { data } = await (late as any).connect.getGmbLocations({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-linked-in-organizations <accountId>',
      'List LinkedIn orgs',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).connect.getLinkedInOrganizations({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-pending-o-auth-data',
      'Get pending OAuth data',
      (y) =>
        y
          .option('token', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['token'] !== undefined) query['token'] = argv['token'];
          const { data } = await (late as any).connect.getPendingOAuthData({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-pinterest-boards <accountId>',
      'List Pinterest boards',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).connect.getPinterestBoards({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-reddit-flairs <accountId>',
      'List subreddit flairs',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('subreddit', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['subreddit'] !== undefined) query['subreddit'] = argv['subreddit'];
          const { data } = await (late as any).connect.getRedditFlairs({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-reddit-subreddits <accountId>',
      'List Reddit subreddits',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).connect.getRedditSubreddits({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-url <platform>',
      'Get OAuth connect URL',
      (y) =>
        y
          .positional('platform', { type: 'string', describe: 'platform', demandOption: true })
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true })
          .option('redirect_url', { type: 'string', describe: 'query param' })
          .option('headless', { type: 'boolean', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { platform: argv['platform'] };
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['redirect_url'] !== undefined) query['redirect_url'] = argv['redirect_url'];
          if (argv['headless'] !== undefined) query['headless'] = argv['headless'];
          const { data } = await (late as any).connect.getConnectUrl({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:get-youtube-playlists <accountId>',
      'List YouTube playlists',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).connect.getYoutubePlaylists({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:handle-o-auth-callback <platform>',
      'Complete OAuth callback',
      (y) =>
        y
          .positional('platform', { type: 'string', describe: 'platform', demandOption: true })
          .option('code', { type: 'string', describe: 'body param', demandOption: true })
          .option('state', { type: 'string', describe: 'body param', demandOption: true })
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { platform: argv['platform'] };
          const body: Record<string, unknown> = {};
          if (argv['code'] !== undefined) body['code'] = argv['code'];
          if (argv['state'] !== undefined) body['state'] = argv['state'];
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          const { data } = await (late as any).connect.handleOAuthCallback({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:list-whats-app-phone-numbers',
      'List WhatsApp phone numbers for selection',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true })
          .option('tempToken', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['tempToken'] !== undefined) query['tempToken'] = argv['tempToken'];
          const { data } = await (late as any).connect.listWhatsAppPhoneNumbers({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:update-facebook-page <accountId>',
      'Update Facebook page',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('selectedPageId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['selectedPageId'] !== undefined) body['selectedPageId'] = argv['selectedPageId'];
          const { data } = await (late as any).connect.updateFacebookPage({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:update-gmb-location <accountId>',
      'Update GBP location',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('selectedLocationId', { type: 'string', describe: 'body param', demandOption: true })
          .option('googleAccountId', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['selectedLocationId'] !== undefined) body['selectedLocationId'] = argv['selectedLocationId'];
          if (argv['googleAccountId'] !== undefined) body['googleAccountId'] = argv['googleAccountId'];
          const { data } = await (late as any).connect.updateGmbLocation({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:update-linked-in-organization <accountId>',
      'Switch LinkedIn account type',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('accountType', { type: 'string', describe: 'body param', demandOption: true })
          .option('selectedOrganization', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountType'] !== undefined) body['accountType'] = argv['accountType'];
          if (argv['selectedOrganization'] !== undefined) body['selectedOrganization'] = JSON.parse(String(argv['selectedOrganization']));
          const { data } = await (late as any).connect.updateLinkedInOrganization({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:update-pinterest-boards <accountId>',
      'Set default Pinterest board',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('defaultBoardId', { type: 'string', describe: 'body param', demandOption: true })
          .option('defaultBoardName', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['defaultBoardId'] !== undefined) body['defaultBoardId'] = argv['defaultBoardId'];
          if (argv['defaultBoardName'] !== undefined) body['defaultBoardName'] = argv['defaultBoardName'];
          const { data } = await (late as any).connect.updatePinterestBoards({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:update-reddit-subreddits <accountId>',
      'Set default subreddit',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('defaultSubreddit', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['defaultSubreddit'] !== undefined) body['defaultSubreddit'] = argv['defaultSubreddit'];
          const { data } = await (late as any).connect.updateRedditSubreddits({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:update-youtube-default-playlist <accountId>',
      'Set default YouTube playlist',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('defaultPlaylistId', { type: 'string', describe: 'body param', demandOption: true })
          .option('defaultPlaylistName', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['defaultPlaylistId'] !== undefined) body['defaultPlaylistId'] = argv['defaultPlaylistId'];
          if (argv['defaultPlaylistName'] !== undefined) body['defaultPlaylistName'] = argv['defaultPlaylistName'];
          const { data } = await (late as any).connect.updateYoutubeDefaultPlaylist({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'connect:whats-app-credentials',
      'Connect WhatsApp via credentials',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true })
          .option('accessToken', { type: 'string', describe: 'body param', demandOption: true })
          .option('wabaId', { type: 'string', describe: 'body param', demandOption: true })
          .option('phoneNumberId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          if (argv['accessToken'] !== undefined) body['accessToken'] = argv['accessToken'];
          if (argv['wabaId'] !== undefined) body['wabaId'] = argv['wabaId'];
          if (argv['phoneNumberId'] !== undefined) body['phoneNumberId'] = argv['phoneNumberId'];
          const { data } = await (late as any).connect.connectWhatsAppCredentials({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:add-member-role <guildId> <userId> <roleId>',
      'Assign a role to a guild member',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .positional('userId', { type: 'string', describe: 'userId', demandOption: true })
          .positional('roleId', { type: 'string', describe: 'roleId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'], userId: argv['userId'], roleId: argv['roleId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.addDiscordMemberRole({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:create-scheduled-event <guildId>',
      'Create a Discord scheduled event',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('description', { type: 'string', describe: 'body param' })
          .option('startsAt', { type: 'string', describe: 'body param', demandOption: true })
          .option('entity', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('imageDataUri', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['startsAt'] !== undefined) body['startsAt'] = argv['startsAt'];
          if (argv['entity'] !== undefined) body['entity'] = JSON.parse(String(argv['entity']));
          if (argv['imageDataUri'] !== undefined) body['imageDataUri'] = argv['imageDataUri'];
          const { data } = await (late as any).discord.createDiscordScheduledEvent({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:delete-scheduled-event <guildId> <eventId>',
      'Delete a Discord scheduled event',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .positional('eventId', { type: 'string', describe: 'eventId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'], eventId: argv['eventId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.deleteDiscordScheduledEvent({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:get-channels <accountId>',
      'List Discord guild channels',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).discord.getDiscordChannels({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:get-scheduled-event <guildId> <eventId>',
      'Get a Discord scheduled event',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .positional('eventId', { type: 'string', describe: 'eventId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'], eventId: argv['eventId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.getDiscordScheduledEvent({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:get-settings <accountId>',
      'Get Discord account settings',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).discord.getDiscordSettings({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:list-guild-members <guildId>',
      'List Discord guild members',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('after', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['after'] !== undefined) query['after'] = argv['after'];
          const { data } = await (late as any).discord.listDiscordGuildMembers({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:list-guild-roles <guildId>',
      'List Discord guild roles',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.listDiscordGuildRoles({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:list-pinned-messages <channelId>',
      'List pinned messages in a Discord channel',
      (y) =>
        y
          .positional('channelId', { type: 'string', describe: 'channelId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { channelId: argv['channelId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.listDiscordPinnedMessages({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:list-scheduled-events <guildId>',
      'List Discord scheduled events',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('withUserCount', { type: 'boolean', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['withUserCount'] !== undefined) query['withUserCount'] = argv['withUserCount'];
          const { data } = await (late as any).discord.listDiscordScheduledEvents({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:pin-message <channelId> <messageId>',
      'Pin a Discord message',
      (y) =>
        y
          .positional('channelId', { type: 'string', describe: 'channelId', demandOption: true })
          .positional('messageId', { type: 'string', describe: 'messageId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { channelId: argv['channelId'], messageId: argv['messageId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.pinDiscordMessage({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:remove-member-role <guildId> <userId> <roleId>',
      'Remove a role from a guild member',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .positional('userId', { type: 'string', describe: 'userId', demandOption: true })
          .positional('roleId', { type: 'string', describe: 'roleId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'], userId: argv['userId'], roleId: argv['roleId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.removeDiscordMemberRole({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:send-direct-message',
      'Send a Discord Direct Message',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('userId', { type: 'string', describe: 'body param', demandOption: true })
          .option('content', { type: 'string', describe: 'body param' })
          .option('embeds', { type: 'string', describe: 'body param (JSON)' })
          .option('attachments', { type: 'string', describe: 'body param (JSON)' })
          .option('tts', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['userId'] !== undefined) body['userId'] = argv['userId'];
          if (argv['content'] !== undefined) body['content'] = argv['content'];
          if (argv['embeds'] !== undefined) body['embeds'] = JSON.parse(String(argv['embeds']));
          if (argv['attachments'] !== undefined) body['attachments'] = JSON.parse(String(argv['attachments']));
          if (argv['tts'] !== undefined) body['tts'] = argv['tts'];
          const { data } = await (late as any).discord.sendDiscordDirectMessage({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:unpin-message <channelId> <messageId>',
      'Unpin a Discord message',
      (y) =>
        y
          .positional('channelId', { type: 'string', describe: 'channelId', demandOption: true })
          .positional('messageId', { type: 'string', describe: 'messageId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { channelId: argv['channelId'], messageId: argv['messageId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).discord.unpinDiscordMessage({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:update-scheduled-event <guildId> <eventId>',
      'Update a Discord scheduled event',
      (y) =>
        y
          .positional('guildId', { type: 'string', describe: 'guildId', demandOption: true })
          .positional('eventId', { type: 'string', describe: 'eventId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param' })
          .option('description', { type: 'string', describe: 'body param' })
          .option('startsAt', { type: 'string', describe: 'body param' })
          .option('endsAt', { type: 'string', describe: 'body param' })
          .option('location', { type: 'string', describe: 'body param' })
          .option('status', { type: 'string', describe: 'body param' })
          .option('imageDataUri', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { guildId: argv['guildId'], eventId: argv['eventId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['startsAt'] !== undefined) body['startsAt'] = argv['startsAt'];
          if (argv['endsAt'] !== undefined) body['endsAt'] = argv['endsAt'];
          if (argv['location'] !== undefined) body['location'] = argv['location'];
          if (argv['status'] !== undefined) body['status'] = argv['status'];
          if (argv['imageDataUri'] !== undefined) body['imageDataUri'] = argv['imageDataUri'];
          const { data } = await (late as any).discord.updateDiscordScheduledEvent({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'discord:update-settings <accountId>',
      'Update Discord settings',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('webhookUsername', { type: 'string', describe: 'body param' })
          .option('webhookAvatarUrl', { type: 'string', describe: 'body param' })
          .option('channelId', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const body: Record<string, unknown> = {};
          if (argv['webhookUsername'] !== undefined) body['webhookUsername'] = argv['webhookUsername'];
          if (argv['webhookAvatarUrl'] !== undefined) body['webhookAvatarUrl'] = argv['webhookAvatarUrl'];
          if (argv['channelId'] !== undefined) body['channelId'] = argv['channelId'];
          const { data } = await (late as any).discord.updateDiscordSettings({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbattributes:get-gmb-attribute-metadata <accountId>',
      'Get attribute metadata',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('categoryName', { type: 'string', describe: 'query param' })
          .option('regionCode', { type: 'string', describe: 'query param' })
          .option('languageCode', { type: 'string', describe: 'query param' })
          .option('pageSize', { type: 'number', describe: 'query param' })
          .option('pageToken', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          if (argv['categoryName'] !== undefined) query['categoryName'] = argv['categoryName'];
          if (argv['regionCode'] !== undefined) query['regionCode'] = argv['regionCode'];
          if (argv['languageCode'] !== undefined) query['languageCode'] = argv['languageCode'];
          if (argv['pageSize'] !== undefined) query['pageSize'] = argv['pageSize'];
          if (argv['pageToken'] !== undefined) query['pageToken'] = argv['pageToken'];
          const { data } = await (late as any).gmbattributes.getGmbAttributeMetadata({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbattributes:get-google-business-attributes <accountId>',
      'Get attributes',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const { data } = await (late as any).gmbattributes.getGoogleBusinessAttributes({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbattributes:update-google-business-attributes <accountId>',
      'Update attributes',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('attributes', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('attributeMask', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['attributes'] !== undefined) body['attributes'] = JSON.parse(String(argv['attributes']));
          if (argv['attributeMask'] !== undefined) body['attributeMask'] = argv['attributeMask'];
          const { data } = await (late as any).gmbattributes.updateGoogleBusinessAttributes({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbfoodmenus:get-google-business-food-menus <accountId>',
      'Get food menus',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const { data } = await (late as any).gmbfoodmenus.getGoogleBusinessFoodMenus({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbfoodmenus:update-google-business-food-menus <accountId>',
      'Update food menus',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('menus', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('updateMask', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['menus'] !== undefined) body['menus'] = JSON.parse(String(argv['menus']));
          if (argv['updateMask'] !== undefined) body['updateMask'] = argv['updateMask'];
          const { data } = await (late as any).gmbfoodmenus.updateGoogleBusinessFoodMenus({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmblocationdetails:get-google-business-location-details <accountId>',
      'Get location details',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('readMask', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          if (argv['readMask'] !== undefined) query['readMask'] = argv['readMask'];
          const { data } = await (late as any).gmblocationdetails.getGoogleBusinessLocationDetails({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmblocationdetails:update-google-business-location-details <accountId>',
      'Update location details',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('updateMask', { type: 'string', describe: 'body param', demandOption: true })
          .option('regularHours', { type: 'string', describe: 'body param (JSON)' })
          .option('specialHours', { type: 'string', describe: 'body param (JSON)' })
          .option('profile', { type: 'string', describe: 'body param (JSON)' })
          .option('websiteUri', { type: 'string', describe: 'body param' })
          .option('phoneNumbers', { type: 'string', describe: 'body param (JSON)' })
          .option('categories', { type: 'string', describe: 'body param (JSON)' })
          .option('serviceItems', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['updateMask'] !== undefined) body['updateMask'] = argv['updateMask'];
          if (argv['regularHours'] !== undefined) body['regularHours'] = JSON.parse(String(argv['regularHours']));
          if (argv['specialHours'] !== undefined) body['specialHours'] = JSON.parse(String(argv['specialHours']));
          if (argv['profile'] !== undefined) body['profile'] = JSON.parse(String(argv['profile']));
          if (argv['websiteUri'] !== undefined) body['websiteUri'] = argv['websiteUri'];
          if (argv['phoneNumbers'] !== undefined) body['phoneNumbers'] = JSON.parse(String(argv['phoneNumbers']));
          if (argv['categories'] !== undefined) body['categories'] = JSON.parse(String(argv['categories']));
          if (argv['serviceItems'] !== undefined) body['serviceItems'] = JSON.parse(String(argv['serviceItems']));
          const { data } = await (late as any).gmblocationdetails.updateGoogleBusinessLocationDetails({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbmedia:create-google-business-media <accountId>',
      'Upload photo',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('sourceUrl', { type: 'string', describe: 'body param', demandOption: true })
          .option('mediaFormat', { type: 'string', describe: 'body param' })
          .option('description', { type: 'string', describe: 'body param' })
          .option('category', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['sourceUrl'] !== undefined) body['sourceUrl'] = argv['sourceUrl'];
          if (argv['mediaFormat'] !== undefined) body['mediaFormat'] = argv['mediaFormat'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['category'] !== undefined) body['category'] = argv['category'];
          const { data } = await (late as any).gmbmedia.createGoogleBusinessMedia({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbmedia:delete-google-business-media <accountId>',
      'Delete photo',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('mediaId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          if (argv['mediaId'] !== undefined) query['mediaId'] = argv['mediaId'];
          const { data } = await (late as any).gmbmedia.deleteGoogleBusinessMedia({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbmedia:list-google-business-media <accountId>',
      'List media',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('pageSize', { type: 'number', describe: 'query param' })
          .option('pageToken', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          if (argv['pageSize'] !== undefined) query['pageSize'] = argv['pageSize'];
          if (argv['pageToken'] !== undefined) query['pageToken'] = argv['pageToken'];
          const { data } = await (late as any).gmbmedia.listGoogleBusinessMedia({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbplaceactions:create-google-business-place-action <accountId>',
      'Create action link',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('uri', { type: 'string', describe: 'body param', demandOption: true })
          .option('placeActionType', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['uri'] !== undefined) body['uri'] = argv['uri'];
          if (argv['placeActionType'] !== undefined) body['placeActionType'] = argv['placeActionType'];
          const { data } = await (late as any).gmbplaceactions.createGoogleBusinessPlaceAction({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbplaceactions:delete-google-business-place-action <accountId>',
      'Delete action link',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('name', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          if (argv['name'] !== undefined) query['name'] = argv['name'];
          const { data } = await (late as any).gmbplaceactions.deleteGoogleBusinessPlaceAction({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbplaceactions:list-google-business-place-actions <accountId>',
      'List action links',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('pageSize', { type: 'number', describe: 'query param' })
          .option('pageToken', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          if (argv['pageSize'] !== undefined) query['pageSize'] = argv['pageSize'];
          if (argv['pageToken'] !== undefined) query['pageToken'] = argv['pageToken'];
          const { data } = await (late as any).gmbplaceactions.listGoogleBusinessPlaceActions({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbplaceactions:update-google-business-place-action <accountId>',
      'Update action link',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('uri', { type: 'string', describe: 'body param' })
          .option('placeActionType', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['uri'] !== undefined) body['uri'] = argv['uri'];
          if (argv['placeActionType'] !== undefined) body['placeActionType'] = argv['placeActionType'];
          const { data } = await (late as any).gmbplaceactions.updateGoogleBusinessPlaceAction({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbservices:get-google-business-services <accountId>',
      'Get services',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const { data } = await (late as any).gmbservices.getGoogleBusinessServices({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbservices:update-google-business-services <accountId>',
      'Replace services',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('serviceItems', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['serviceItems'] !== undefined) body['serviceItems'] = JSON.parse(String(argv['serviceItems']));
          const { data } = await (late as any).gmbservices.updateGoogleBusinessServices({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbverifications:complete-google-business-verification <accountId> <verificationId>',
      'Complete a verification',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('verificationId', { type: 'string', describe: 'verificationId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('pin', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], verificationId: argv['verificationId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['pin'] !== undefined) body['pin'] = argv['pin'];
          const { data } = await (late as any).gmbverifications.completeGoogleBusinessVerification({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbverifications:fetch-google-business-verification-options <accountId>',
      'Fetch verification options',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('languageCode', { type: 'string', describe: 'body param', demandOption: true })
          .option('context', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['languageCode'] !== undefined) body['languageCode'] = argv['languageCode'];
          if (argv['context'] !== undefined) body['context'] = JSON.parse(String(argv['context']));
          const { data } = await (late as any).gmbverifications.fetchGoogleBusinessVerificationOptions({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbverifications:get-google-business-verifications <accountId>',
      'Get verification state',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const { data } = await (late as any).gmbverifications.getGoogleBusinessVerifications({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'gmbverifications:start-google-business-verification <accountId>',
      'Start a verification',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .option('locationId', { type: 'string', describe: 'query param' })
          .option('method', { type: 'string', describe: 'body param', demandOption: true })
          .option('languageCode', { type: 'string', describe: 'body param' })
          .option('phoneNumber', { type: 'string', describe: 'body param' })
          .option('emailAddress', { type: 'string', describe: 'body param' })
          .option('mailerContact', { type: 'string', describe: 'body param (JSON)' })
          .option('context', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const query: Record<string, unknown> = {};
          if (argv['locationId'] !== undefined) query['locationId'] = argv['locationId'];
          const body: Record<string, unknown> = {};
          if (argv['method'] !== undefined) body['method'] = argv['method'];
          if (argv['languageCode'] !== undefined) body['languageCode'] = argv['languageCode'];
          if (argv['phoneNumber'] !== undefined) body['phoneNumber'] = argv['phoneNumber'];
          if (argv['emailAddress'] !== undefined) body['emailAddress'] = argv['emailAddress'];
          if (argv['mailerContact'] !== undefined) body['mailerContact'] = JSON.parse(String(argv['mailerContact']));
          if (argv['context'] !== undefined) body['context'] = JSON.parse(String(argv['context']));
          const { data } = await (late as any).gmbverifications.startGoogleBusinessVerification({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'inboxanalytics:get-inbox-conversation-analytics <conversationId>',
      'Get analytics for a single conversation',
      (y) =>
        y
          .positional('conversationId', { type: 'string', describe: 'conversationId', demandOption: true })
          .option('fromDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('toDate', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { conversationId: argv['conversationId'] };
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          const { data } = await (late as any).inboxanalytics.getInboxConversationAnalytics({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'inboxanalytics:get-inbox-heatmap',
      'Get inbox day-of-week × hour-of-day heatmap',
      (y) =>
        y
          .option('fromDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' })
          .option('action', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          if (argv['action'] !== undefined) query['action'] = argv['action'];
          const { data } = await (late as any).inboxanalytics.getInboxHeatmap({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'inboxanalytics:get-inbox-response-time',
      'Get inbox response-time stats',
      (y) =>
        y
          .option('fromDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).inboxanalytics.getInboxResponseTime({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'inboxanalytics:get-inbox-source-breakdown',
      'Get inbox source breakdown',
      (y) =>
        y
          .option('fromDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).inboxanalytics.getInboxSourceBreakdown({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'inboxanalytics:get-inbox-top-accounts',
      'Get top accounts by inbox volume',
      (y) =>
        y
          .option('fromDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          const { data } = await (late as any).inboxanalytics.getInboxTopAccounts({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'inboxanalytics:get-inbox-volume',
      'Get inbox messaging volume',
      (y) =>
        y
          .option('fromDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          const { data } = await (late as any).inboxanalytics.getInboxVolume({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'inboxanalytics:list-inbox-conversation-analytics',
      'List conversations with inbox analytics',
      (y) =>
        y
          .option('fromDate', { type: 'string', describe: 'query param', demandOption: true })
          .option('toDate', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('platform', { type: 'string', describe: 'query param' })
          .option('accountId', { type: 'string', describe: 'query param' })
          .option('source', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('page', { type: 'number', describe: 'query param' })
          .option('sortBy', { type: 'string', describe: 'query param' })
          .option('order', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['fromDate'] !== undefined) query['fromDate'] = argv['fromDate'];
          if (argv['toDate'] !== undefined) query['toDate'] = argv['toDate'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['platform'] !== undefined) query['platform'] = argv['platform'];
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['source'] !== undefined) query['source'] = argv['source'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['page'] !== undefined) query['page'] = argv['page'];
          if (argv['sortBy'] !== undefined) query['sortBy'] = argv['sortBy'];
          if (argv['order'] !== undefined) query['order'] = argv['order'];
          const { data } = await (late as any).inboxanalytics.listInboxConversationAnalytics({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'instagram:get-story-insights <accountId> <storyId>',
      'Get Instagram story insights',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true })
          .positional('storyId', { type: 'string', describe: 'storyId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'], storyId: argv['storyId'] };
          const { data } = await (late as any).instagram.getInstagramStoryInsights({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'instagram:list-stories <accountId>',
      'List active Instagram stories',
      (y) =>
        y
          .positional('accountId', { type: 'string', describe: 'accountId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { accountId: argv['accountId'] };
          const { data } = await (late as any).instagram.listInstagramStories({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'invites:create-token',
      'Create invite token',
      (y) =>
        y
          .option('scope', { type: 'string', describe: 'body param', demandOption: true })
          .option('profileIds', { type: 'string', describe: 'body param (comma-separated)' })
          .option('role', { type: 'string', describe: 'body param' })
          .option('readOnly', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['scope'] !== undefined) body['scope'] = argv['scope'];
          if (argv['profileIds'] !== undefined) body['profileIds'] = String(argv['profileIds']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['role'] !== undefined) body['role'] = argv['role'];
          if (argv['readOnly'] !== undefined) body['readOnly'] = argv['readOnly'];
          const { data } = await (late as any).invites.createInviteToken({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'messages:upload-media-direct',
      'Upload media file',
      (y) => y,
      async (argv) => {
        try {
          const late = createClient();

          const { data } = await (late as any).messages.uploadMediaDirect();
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'queue:create-slot',
      'Create schedule',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('timezone', { type: 'string', describe: 'body param', demandOption: true })
          .option('slots', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('active', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['timezone'] !== undefined) body['timezone'] = argv['timezone'];
          if (argv['slots'] !== undefined) body['slots'] = JSON.parse(String(argv['slots']));
          if (argv['active'] !== undefined) body['active'] = argv['active'];
          const { data } = await (late as any).queue.createQueueSlot({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'queue:delete-slot',
      'Delete schedule',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true })
          .option('queueId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['queueId'] !== undefined) query['queueId'] = argv['queueId'];
          const { data } = await (late as any).queue.deleteQueueSlot({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'queue:get-next-slot',
      'Get next available slot',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true })
          .option('queueId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['queueId'] !== undefined) query['queueId'] = argv['queueId'];
          const { data } = await (late as any).queue.getNextQueueSlot({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'queue:list-slots',
      'List schedules',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true })
          .option('queueId', { type: 'string', describe: 'query param' })
          .option('all', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['queueId'] !== undefined) query['queueId'] = argv['queueId'];
          if (argv['all'] !== undefined) query['all'] = argv['all'];
          const { data } = await (late as any).queue.listQueueSlots({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'queue:preview',
      'Preview upcoming slots',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true })
          .option('queueId', { type: 'string', describe: 'query param' })
          .option('count', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['queueId'] !== undefined) query['queueId'] = argv['queueId'];
          if (argv['count'] !== undefined) query['count'] = argv['count'];
          const { data } = await (late as any).queue.previewQueue({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'queue:update-slot',
      'Update schedule',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true })
          .option('queueId', { type: 'string', describe: 'body param' })
          .option('name', { type: 'string', describe: 'body param' })
          .option('timezone', { type: 'string', describe: 'body param', demandOption: true })
          .option('slots', { type: 'string', describe: 'body param (JSON)', demandOption: true })
          .option('active', { type: 'boolean', describe: 'body param' })
          .option('setAsDefault', { type: 'boolean', describe: 'body param' })
          .option('reshuffleExisting', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          if (argv['queueId'] !== undefined) body['queueId'] = argv['queueId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['timezone'] !== undefined) body['timezone'] = argv['timezone'];
          if (argv['slots'] !== undefined) body['slots'] = JSON.parse(String(argv['slots']));
          if (argv['active'] !== undefined) body['active'] = argv['active'];
          if (argv['setAsDefault'] !== undefined) body['setAsDefault'] = argv['setAsDefault'];
          if (argv['reshuffleExisting'] !== undefined) body['reshuffleExisting'] = argv['reshuffleExisting'];
          const { data } = await (late as any).queue.updateQueueSlot({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'reddit:get-feed',
      'Get subreddit feed',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('subreddit', { type: 'string', describe: 'query param' })
          .option('sort', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('after', { type: 'string', describe: 'query param' })
          .option('t', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['subreddit'] !== undefined) query['subreddit'] = argv['subreddit'];
          if (argv['sort'] !== undefined) query['sort'] = argv['sort'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['after'] !== undefined) query['after'] = argv['after'];
          if (argv['t'] !== undefined) query['t'] = argv['t'];
          const { data } = await (late as any).reddit.getRedditFeed({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'reddit:search',
      'Search posts',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('subreddit', { type: 'string', describe: 'query param' })
          .option('q', { type: 'string', describe: 'query param', demandOption: true })
          .option('restrict_sr', { type: 'string', describe: 'query param' })
          .option('sort', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('after', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['subreddit'] !== undefined) query['subreddit'] = argv['subreddit'];
          if (argv['q'] !== undefined) query['q'] = argv['q'];
          if (argv['restrict_sr'] !== undefined) query['restrict_sr'] = argv['restrict_sr'];
          if (argv['sort'] !== undefined) query['sort'] = argv['sort'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['after'] !== undefined) query['after'] = argv['after'];
          const { data } = await (late as any).reddit.searchReddit({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'twitterengagement:bookmark-post',
      'Bookmark a tweet',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('tweetId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['tweetId'] !== undefined) body['tweetId'] = argv['tweetId'];
          const { data } = await (late as any).twitterengagement.bookmarkPost({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'twitterengagement:follow-user',
      'Follow a user',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('targetUserId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['targetUserId'] !== undefined) body['targetUserId'] = argv['targetUserId'];
          const { data } = await (late as any).twitterengagement.followUser({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'twitterengagement:remove-bookmark',
      'Remove bookmark',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('tweetId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['tweetId'] !== undefined) query['tweetId'] = argv['tweetId'];
          const { data } = await (late as any).twitterengagement.removeBookmark({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'twitterengagement:retweet-post',
      'Retweet a post',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('tweetId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['tweetId'] !== undefined) body['tweetId'] = argv['tweetId'];
          const { data } = await (late as any).twitterengagement.retweetPost({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'twitterengagement:undo-retweet',
      'Undo retweet',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('tweetId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['tweetId'] !== undefined) query['tweetId'] = argv['tweetId'];
          const { data } = await (late as any).twitterengagement.undoRetweet({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'twitterengagement:unfollow-user',
      'Unfollow a user',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('targetUserId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['targetUserId'] !== undefined) query['targetUserId'] = argv['targetUserId'];
          const { data } = await (late as any).twitterengagement.unfollowUser({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'users:get <userId>',
      'Get user',
      (y) =>
        y
          .positional('userId', { type: 'string', describe: 'userId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { userId: argv['userId'] };
          const { data } = await (late as any).users.getUser({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'webhooks:create-settings',
      'Create webhook',
      (y) =>
        y
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('url', { type: 'string', describe: 'body param', demandOption: true })
          .option('secret', { type: 'string', describe: 'body param' })
          .option('events', { type: 'string', describe: 'body param (comma-separated)', demandOption: true })
          .option('isActive', { type: 'boolean', describe: 'body param' })
          .option('customHeaders', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['url'] !== undefined) body['url'] = argv['url'];
          if (argv['secret'] !== undefined) body['secret'] = argv['secret'];
          if (argv['events'] !== undefined) body['events'] = String(argv['events']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['isActive'] !== undefined) body['isActive'] = argv['isActive'];
          if (argv['customHeaders'] !== undefined) body['customHeaders'] = JSON.parse(String(argv['customHeaders']));
          const { data } = await (late as any).webhooks.createWebhookSettings({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'webhooks:delete-settings',
      'Delete webhook',
      (y) =>
        y
          .option('id', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['id'] !== undefined) query['id'] = argv['id'];
          const { data } = await (late as any).webhooks.deleteWebhookSettings({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'webhooks:get-logs',
      'List webhook delivery logs',
      (y) =>
        y
          .option('limit', { type: 'number', describe: 'query param' })
          .option('skip', { type: 'number', describe: 'query param' })
          .option('status', { type: 'string', describe: 'query param' })
          .option('event', { type: 'string', describe: 'query param' })
          .option('webhookId', { type: 'string', describe: 'query param' })
          .option('eventId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['skip'] !== undefined) query['skip'] = argv['skip'];
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['event'] !== undefined) query['event'] = argv['event'];
          if (argv['webhookId'] !== undefined) query['webhookId'] = argv['webhookId'];
          if (argv['eventId'] !== undefined) query['eventId'] = argv['eventId'];
          const { data } = await (late as any).webhooks.getWebhookLogs({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'webhooks:get-settings',
      'List webhooks',
      (y) => y,
      async (argv) => {
        try {
          const late = createClient();

          const { data } = await (late as any).webhooks.getWebhookSettings();
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'webhooks:test',
      'Send test webhook',
      (y) =>
        y
          .option('webhookId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['webhookId'] !== undefined) body['webhookId'] = argv['webhookId'];
          const { data } = await (late as any).webhooks.testWebhook({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'webhooks:update-settings',
      'Update webhook',
      (y) =>
        y
          .option('_id', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param' })
          .option('url', { type: 'string', describe: 'body param' })
          .option('secret', { type: 'string', describe: 'body param' })
          .option('events', { type: 'string', describe: 'body param (comma-separated)' })
          .option('isActive', { type: 'boolean', describe: 'body param' })
          .option('customHeaders', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['_id'] !== undefined) body['_id'] = argv['_id'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['url'] !== undefined) body['url'] = argv['url'];
          if (argv['secret'] !== undefined) body['secret'] = argv['secret'];
          if (argv['events'] !== undefined) body['events'] = String(argv['events']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['isActive'] !== undefined) body['isActive'] = argv['isActive'];
          if (argv['customHeaders'] !== undefined) body['customHeaders'] = JSON.parse(String(argv['customHeaders']));
          const { data } = await (late as any).webhooks.updateWebhookSettings({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:add-whats-app-group-participants <groupId>',
      'Add participants',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('phoneNumbers', { type: 'string', describe: 'body param (comma-separated)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const body: Record<string, unknown> = {};
          if (argv['phoneNumbers'] !== undefined) body['phoneNumbers'] = String(argv['phoneNumbers']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsapp.addWhatsAppGroupParticipants({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:approve-whats-app-group-join-requests <groupId>',
      'Approve join requests',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('phoneNumbers', { type: 'string', describe: 'body param (comma-separated)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const body: Record<string, unknown> = {};
          if (argv['phoneNumbers'] !== undefined) body['phoneNumbers'] = String(argv['phoneNumbers']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsapp.approveWhatsAppGroupJoinRequests({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:block-whats-app-users',
      'Block users',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('users', { type: 'string', describe: 'body param (comma-separated)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['users'] !== undefined) body['users'] = String(argv['users']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsapp.blockWhatsAppUsers({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:create-whats-app-dataset',
      'Provision CTWA conversions dataset',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.createWhatsAppDataset({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:create-whats-app-group-chat',
      'Create group',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('subject', { type: 'string', describe: 'body param', demandOption: true })
          .option('description', { type: 'string', describe: 'body param' })
          .option('joinApprovalMode', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['subject'] !== undefined) body['subject'] = argv['subject'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['joinApprovalMode'] !== undefined) body['joinApprovalMode'] = argv['joinApprovalMode'];
          const { data } = await (late as any).whatsapp.createWhatsAppGroupChat({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:create-whats-app-group-invite-link <groupId>',
      'Create invite link',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.createWhatsAppGroupInviteLink({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:create-whats-app-template',
      'Create template',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('category', { type: 'string', describe: 'body param', demandOption: true })
          .option('language', { type: 'string', describe: 'body param', demandOption: true })
          .option('components', { type: 'string', describe: 'body param (JSON)' })
          .option('library_template_name', { type: 'string', describe: 'body param' })
          .option('library_template_body_inputs', { type: 'string', describe: 'body param (JSON)' })
          .option('library_template_button_inputs', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['category'] !== undefined) body['category'] = argv['category'];
          if (argv['language'] !== undefined) body['language'] = argv['language'];
          if (argv['components'] !== undefined) body['components'] = JSON.parse(String(argv['components']));
          if (argv['library_template_name'] !== undefined) body['library_template_name'] = argv['library_template_name'];
          if (argv['library_template_body_inputs'] !== undefined) body['library_template_body_inputs'] = JSON.parse(String(argv['library_template_body_inputs']));
          if (argv['library_template_button_inputs'] !== undefined) body['library_template_button_inputs'] = JSON.parse(String(argv['library_template_button_inputs']));
          const { data } = await (late as any).whatsapp.createWhatsAppTemplate({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:delete-whats-app-group-chat <groupId>',
      'Delete group',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.deleteWhatsAppGroupChat({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:delete-whats-app-template <templateName>',
      'Delete template',
      (y) =>
        y
          .positional('templateName', { type: 'string', describe: 'templateName', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { templateName: argv['templateName'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.deleteWhatsAppTemplate({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-block-status',
      'Check if a user is blocked',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('user', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['user'] !== undefined) query['user'] = argv['user'];
          const { data } = await (late as any).whatsapp.getWhatsAppBlockStatus({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-blocked-users',
      'List blocked users',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('after', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['after'] !== undefined) query['after'] = argv['after'];
          const { data } = await (late as any).whatsapp.getWhatsAppBlockedUsers({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-business-profile',
      'Get business profile',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.getWhatsAppBusinessProfile({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-dataset',
      'Get CTWA conversions dataset',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.getWhatsAppDataset({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-display-name',
      'Get display name status',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.getWhatsAppDisplayName({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-group-chat <groupId>',
      'Get group info',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.getWhatsAppGroupChat({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-template <templateName>',
      'Get template',
      (y) =>
        y
          .positional('templateName', { type: 'string', describe: 'templateName', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { templateName: argv['templateName'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.getWhatsAppTemplate({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:get-whats-app-templates',
      'List templates',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.getWhatsAppTemplates({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:list-whats-app-conversions',
      'List recent WhatsApp conversion events',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('limit', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          const { data } = await (late as any).whatsapp.listWhatsAppConversions({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:list-whats-app-group-chats',
      'List active groups',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('after', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['after'] !== undefined) query['after'] = argv['after'];
          const { data } = await (late as any).whatsapp.listWhatsAppGroupChats({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:list-whats-app-group-join-requests <groupId>',
      'List join requests',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsapp.listWhatsAppGroupJoinRequests({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:reject-whats-app-group-join-requests <groupId>',
      'Reject join requests',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('phoneNumbers', { type: 'string', describe: 'body param (comma-separated)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const body: Record<string, unknown> = {};
          if (argv['phoneNumbers'] !== undefined) body['phoneNumbers'] = String(argv['phoneNumbers']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsapp.rejectWhatsAppGroupJoinRequests({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:remove-whats-app-group-participants <groupId>',
      'Remove participants',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('phoneNumbers', { type: 'string', describe: 'body param (comma-separated)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const body: Record<string, unknown> = {};
          if (argv['phoneNumbers'] !== undefined) body['phoneNumbers'] = String(argv['phoneNumbers']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsapp.removeWhatsAppGroupParticipants({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:send-whats-app-conversion',
      'Send WhatsApp conversion event',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('eventName', { type: 'string', describe: 'body param', demandOption: true })
          .option('eventTime', { type: 'number', describe: 'body param' })
          .option('eventId', { type: 'string', describe: 'body param', demandOption: true })
          .option('conversationId', { type: 'string', describe: 'body param' })
          .option('phoneE164', { type: 'string', describe: 'body param' })
          .option('value', { type: 'number', describe: 'body param' })
          .option('currency', { type: 'string', describe: 'body param' })
          .option('contentIds', { type: 'string', describe: 'body param (comma-separated)' })
          .option('email', { type: 'string', describe: 'body param' })
          .option('externalId', { type: 'string', describe: 'body param' })
          .option('testCode', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['eventName'] !== undefined) body['eventName'] = argv['eventName'];
          if (argv['eventTime'] !== undefined) body['eventTime'] = argv['eventTime'];
          if (argv['eventId'] !== undefined) body['eventId'] = argv['eventId'];
          if (argv['conversationId'] !== undefined) body['conversationId'] = argv['conversationId'];
          if (argv['phoneE164'] !== undefined) body['phoneE164'] = argv['phoneE164'];
          if (argv['value'] !== undefined) body['value'] = argv['value'];
          if (argv['currency'] !== undefined) body['currency'] = argv['currency'];
          if (argv['contentIds'] !== undefined) body['contentIds'] = String(argv['contentIds']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['email'] !== undefined) body['email'] = argv['email'];
          if (argv['externalId'] !== undefined) body['externalId'] = argv['externalId'];
          if (argv['testCode'] !== undefined) body['testCode'] = argv['testCode'];
          const { data } = await (late as any).whatsapp.sendWhatsAppConversion({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:unblock-whats-app-users',
      'Unblock users',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('users', { type: 'string', describe: 'body param (comma-separated)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['users'] !== undefined) body['users'] = String(argv['users']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsapp.unblockWhatsAppUsers({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:update-whats-app-business-profile',
      'Update business profile',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('about', { type: 'string', describe: 'body param' })
          .option('address', { type: 'string', describe: 'body param' })
          .option('description', { type: 'string', describe: 'body param' })
          .option('email', { type: 'string', describe: 'body param' })
          .option('websites', { type: 'string', describe: 'body param (comma-separated)' })
          .option('vertical', { type: 'string', describe: 'body param' })
          .option('profilePictureHandle', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['about'] !== undefined) body['about'] = argv['about'];
          if (argv['address'] !== undefined) body['address'] = argv['address'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['email'] !== undefined) body['email'] = argv['email'];
          if (argv['websites'] !== undefined) body['websites'] = String(argv['websites']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['vertical'] !== undefined) body['vertical'] = argv['vertical'];
          if (argv['profilePictureHandle'] !== undefined) body['profilePictureHandle'] = argv['profilePictureHandle'];
          const { data } = await (late as any).whatsapp.updateWhatsAppBusinessProfile({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:update-whats-app-display-name',
      'Request display name change',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('displayName', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['displayName'] !== undefined) body['displayName'] = argv['displayName'];
          const { data } = await (late as any).whatsapp.updateWhatsAppDisplayName({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:update-whats-app-group-chat <groupId>',
      'Update group settings',
      (y) =>
        y
          .positional('groupId', { type: 'string', describe: 'groupId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('subject', { type: 'string', describe: 'body param' })
          .option('description', { type: 'string', describe: 'body param' })
          .option('joinApprovalMode', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { groupId: argv['groupId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const body: Record<string, unknown> = {};
          if (argv['subject'] !== undefined) body['subject'] = argv['subject'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['joinApprovalMode'] !== undefined) body['joinApprovalMode'] = argv['joinApprovalMode'];
          const { data } = await (late as any).whatsapp.updateWhatsAppGroupChat({ path, query: query as any, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:update-whats-app-template <templateName>',
      'Update template',
      (y) =>
        y
          .positional('templateName', { type: 'string', describe: 'templateName', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('components', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { templateName: argv['templateName'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['components'] !== undefined) body['components'] = JSON.parse(String(argv['components']));
          const { data } = await (late as any).whatsapp.updateWhatsAppTemplate({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapp:upload-whats-app-profile-photo',
      'Upload profile picture',
      (y) => y,
      async (argv) => {
        try {
          const late = createClient();

          const { data } = await (late as any).whatsapp.uploadWhatsAppProfilePhoto();
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:disable-whats-app-calling <id>',
      'Disable calling on a number',
      (y) =>
        y
          .positional('id', { type: 'string', describe: 'id', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { id: argv['id'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappcalling.disableWhatsAppCalling({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:enable-whats-app-calling <id>',
      'Enable calling on a number',
      (y) =>
        y
          .positional('id', { type: 'string', describe: 'id', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('forwardTo', { type: 'string', describe: 'body param', demandOption: true })
          .option('sipAuthUsername', { type: 'string', describe: 'body param' })
          .option('sipAuthPassword', { type: 'string', describe: 'body param' })
          .option('recordingEnabled', { type: 'boolean', describe: 'body param' })
          .option('callIconCountries', { type: 'string', describe: 'body param (comma-separated)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { id: argv['id'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['forwardTo'] !== undefined) body['forwardTo'] = argv['forwardTo'];
          if (argv['sipAuthUsername'] !== undefined) body['sipAuthUsername'] = argv['sipAuthUsername'];
          if (argv['sipAuthPassword'] !== undefined) body['sipAuthPassword'] = argv['sipAuthPassword'];
          if (argv['recordingEnabled'] !== undefined) body['recordingEnabled'] = argv['recordingEnabled'];
          if (argv['callIconCountries'] !== undefined) body['callIconCountries'] = String(argv['callIconCountries']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsappcalling.enableWhatsAppCalling({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:get-whats-app-call <callId>',
      'Get a single call',
      (y) =>
        y
          .positional('callId', { type: 'string', describe: 'callId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { callId: argv['callId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappcalling.getWhatsAppCall({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:get-whats-app-call-estimate',
      'Estimate per-minute cost for a destination',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('to', { type: 'string', describe: 'query param', demandOption: true })
          .option('minutes', { type: 'number', describe: 'query param' })
          .option('recording', { type: 'boolean', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['to'] !== undefined) query['to'] = argv['to'];
          if (argv['minutes'] !== undefined) query['minutes'] = argv['minutes'];
          if (argv['recording'] !== undefined) query['recording'] = argv['recording'];
          const { data } = await (late as any).whatsappcalling.getWhatsAppCallEstimate({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:get-whats-app-call-permissions',
      'Check call permission for a consumer',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('to', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['to'] !== undefined) query['to'] = argv['to'];
          const { data } = await (late as any).whatsappcalling.getWhatsAppCallPermissions({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:get-whats-app-calling-config',
      'Get calling config for an account',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappcalling.getWhatsAppCallingConfig({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:initiate-whats-app-call',
      'Initiate outbound call',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('to', { type: 'string', describe: 'body param', demandOption: true })
          .option('action', { type: 'string', describe: 'body param' })
          .option('bodyText', { type: 'string', describe: 'body param' })
          .option('forwardTo', { type: 'string', describe: 'body param' })
          .option('recordOverride', { type: 'boolean', describe: 'body param' })
          .option('biz_opaque_callback_data', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['to'] !== undefined) body['to'] = argv['to'];
          if (argv['action'] !== undefined) body['action'] = argv['action'];
          if (argv['bodyText'] !== undefined) body['bodyText'] = argv['bodyText'];
          if (argv['forwardTo'] !== undefined) body['forwardTo'] = argv['forwardTo'];
          if (argv['recordOverride'] !== undefined) body['recordOverride'] = argv['recordOverride'];
          if (argv['biz_opaque_callback_data'] !== undefined) body['biz_opaque_callback_data'] = argv['biz_opaque_callback_data'];
          const { data } = await (late as any).whatsappcalling.initiateWhatsAppCall({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:list-whats-app-calls',
      'List call history for an account',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('status', { type: 'string', describe: 'query param' })
          .option('direction', { type: 'string', describe: 'query param' })
          .option('since', { type: 'string', describe: 'query param' })
          .option('until', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['direction'] !== undefined) query['direction'] = argv['direction'];
          if (argv['since'] !== undefined) query['since'] = argv['since'];
          if (argv['until'] !== undefined) query['until'] = argv['until'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          const { data } = await (late as any).whatsappcalling.listWhatsAppCalls({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappcalling:update-whats-app-calling <id>',
      'Update calling config',
      (y) =>
        y
          .positional('id', { type: 'string', describe: 'id', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('forwardTo', { type: 'string', describe: 'body param' })
          .option('sipAuthUsername', { type: 'string', describe: 'body param' })
          .option('sipAuthPassword', { type: 'string', describe: 'body param' })
          .option('recordingEnabled', { type: 'boolean', describe: 'body param' })
          .option('callIconCountries', { type: 'string', describe: 'body param (comma-separated)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { id: argv['id'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['forwardTo'] !== undefined) body['forwardTo'] = argv['forwardTo'];
          if (argv['sipAuthUsername'] !== undefined) body['sipAuthUsername'] = argv['sipAuthUsername'];
          if (argv['sipAuthPassword'] !== undefined) body['sipAuthPassword'] = argv['sipAuthPassword'];
          if (argv['recordingEnabled'] !== undefined) body['recordingEnabled'] = argv['recordingEnabled'];
          if (argv['callIconCountries'] !== undefined) body['callIconCountries'] = String(argv['callIconCountries']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsappcalling.updateWhatsAppCalling({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:create-whats-app-flow',
      'Create flow',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('categories', { type: 'string', describe: 'body param (comma-separated)', demandOption: true })
          .option('cloneFlowId', { type: 'string', describe: 'body param' })
          .option('asVersion', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['categories'] !== undefined) body['categories'] = String(argv['categories']).split(',').map((s) => s.trim()).filter(Boolean);
          if (argv['cloneFlowId'] !== undefined) body['cloneFlowId'] = argv['cloneFlowId'];
          if (argv['asVersion'] !== undefined) body['asVersion'] = argv['asVersion'];
          const { data } = await (late as any).whatsappflows.createWhatsAppFlow({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:delete-whats-app-flow <flowId>',
      'Delete flow',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappflows.deleteWhatsAppFlow({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:deprecate-whats-app-flow <flowId>',
      'Deprecate flow',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappflows.deprecateWhatsAppFlow({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:get-whats-app-flow <flowId>',
      'Get flow',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('fields', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['fields'] !== undefined) query['fields'] = argv['fields'];
          const { data } = await (late as any).whatsappflows.getWhatsAppFlow({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:get-whats-app-flow-json <flowId>',
      'Get flow JSON asset',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappflows.getWhatsAppFlowJson({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:get-whats-app-flow-preview <flowId>',
      'Get flow preview URL',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('invalidate', { type: 'boolean', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['invalidate'] !== undefined) query['invalidate'] = argv['invalidate'];
          const { data } = await (late as any).whatsappflows.getWhatsAppFlowPreview({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:list-whats-app-flow-responses',
      'List flow responses',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('flowId', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['flowId'] !== undefined) query['flowId'] = argv['flowId'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          const { data } = await (late as any).whatsappflows.listWhatsAppFlowResponses({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:list-whats-app-flow-versions <flowId>',
      'List flow versions',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappflows.listWhatsAppFlowVersions({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:list-whats-app-flows',
      'List flows',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappflows.listWhatsAppFlows({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:publish-whats-app-flow <flowId>',
      'Publish flow',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappflows.publishWhatsAppFlow({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:send-whats-app-flow-message',
      'Send flow message',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('to', { type: 'string', describe: 'body param', demandOption: true })
          .option('flow_id', { type: 'string', describe: 'body param', demandOption: true })
          .option('flow_cta', { type: 'string', describe: 'body param', demandOption: true })
          .option('flow_action', { type: 'string', describe: 'body param' })
          .option('flow_token', { type: 'string', describe: 'body param' })
          .option('flow_action_payload', { type: 'string', describe: 'body param (JSON)' })
          .option('body', { type: 'string', describe: 'body param', demandOption: true })
          .option('header', { type: 'string', describe: 'body param (JSON)' })
          .option('footer', { type: 'string', describe: 'body param' })
          .option('draft', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['to'] !== undefined) body['to'] = argv['to'];
          if (argv['flow_id'] !== undefined) body['flow_id'] = argv['flow_id'];
          if (argv['flow_cta'] !== undefined) body['flow_cta'] = argv['flow_cta'];
          if (argv['flow_action'] !== undefined) body['flow_action'] = argv['flow_action'];
          if (argv['flow_token'] !== undefined) body['flow_token'] = argv['flow_token'];
          if (argv['flow_action_payload'] !== undefined) body['flow_action_payload'] = JSON.parse(String(argv['flow_action_payload']));
          if (argv['body'] !== undefined) body['body'] = argv['body'];
          if (argv['header'] !== undefined) body['header'] = JSON.parse(String(argv['header']));
          if (argv['footer'] !== undefined) body['footer'] = argv['footer'];
          if (argv['draft'] !== undefined) body['draft'] = argv['draft'];
          const { data } = await (late as any).whatsappflows.sendWhatsAppFlowMessage({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:update-whats-app-flow <flowId>',
      'Update flow',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('name', { type: 'string', describe: 'body param' })
          .option('categories', { type: 'string', describe: 'body param (comma-separated)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['categories'] !== undefined) body['categories'] = String(argv['categories']).split(',').map((s) => s.trim()).filter(Boolean);
          const { data } = await (late as any).whatsappflows.updateWhatsAppFlow({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappflows:upload-whats-app-flow-json <flowId>',
      'Upload flow JSON',
      (y) =>
        y
          .positional('flowId', { type: 'string', describe: 'flowId', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('flow_json', { type: 'string', describe: 'body param (JSON)', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { flowId: argv['flowId'] };
          const body: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['flow_json'] !== undefined) body['flow_json'] = JSON.parse(String(argv['flow_json']));
          const { data } = await (late as any).whatsappflows.uploadWhatsAppFlowJson({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:check-whats-app-number-availability',
      'Check a country\'s availability + address constraint',
      (y) =>
        y
          .option('country', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['country'] !== undefined) query['country'] = argv['country'];
          const { data } = await (late as any).whatsappphonenumbers.checkWhatsAppNumberAvailability({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:get-whats-app-number-info',
      'Get number status',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          const { data } = await (late as any).whatsappphonenumbers.getWhatsAppNumberInfo({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:get-whats-app-number-kyc-form',
      'Get regulated-number KYC form spec',
      (y) =>
        y
          .option('country', { type: 'string', describe: 'query param', demandOption: true })
          .option('profileId', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['country'] !== undefined) query['country'] = argv['country'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          const { data } = await (late as any).whatsappphonenumbers.getWhatsAppNumberKycForm({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:get-whats-app-number-remediation <id>',
      'Get the declined requirements to fix',
      (y) =>
        y
          .positional('id', { type: 'string', describe: 'id', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { id: argv['id'] };
          const { data } = await (late as any).whatsappphonenumbers.getWhatsAppNumberRemediation({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:get-whats-app-phone-number <phoneNumberId>',
      'Get phone number',
      (y) =>
        y
          .positional('phoneNumberId', { type: 'string', describe: 'phoneNumberId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { phoneNumberId: argv['phoneNumberId'] };
          const { data } = await (late as any).whatsappphonenumbers.getWhatsAppPhoneNumber({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:get-whats-app-phone-numbers',
      'List phone numbers',
      (y) =>
        y
          .option('status', { type: 'string', describe: 'query param' })
          .option('profileId', { type: 'string', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          const { data } = await (late as any).whatsappphonenumbers.getWhatsAppPhoneNumbers({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:list-whats-app-number-countries',
      'List offerable number countries',
      (y) => y,
      async (argv) => {
        try {
          const late = createClient();

          const { data } = await (late as any).whatsappphonenumbers.listWhatsAppNumberCountries();
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:purchase-whats-app-phone-number',
      'Purchase phone number',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true })
          .option('country', { type: 'string', describe: 'body param' })
          .option('purchaseIntentId', { type: 'string', describe: 'body param' })
          .option('allowMultiple', { type: 'boolean', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          if (argv['country'] !== undefined) body['country'] = argv['country'];
          if (argv['purchaseIntentId'] !== undefined) body['purchaseIntentId'] = argv['purchaseIntentId'];
          if (argv['allowMultiple'] !== undefined) body['allowMultiple'] = argv['allowMultiple'];
          const { data } = await (late as any).whatsappphonenumbers.purchaseWhatsAppPhoneNumber({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:release-whats-app-phone-number <phoneNumberId>',
      'Release phone number',
      (y) =>
        y
          .positional('phoneNumberId', { type: 'string', describe: 'phoneNumberId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { phoneNumberId: argv['phoneNumberId'] };
          const { data } = await (late as any).whatsappphonenumbers.releaseWhatsAppPhoneNumber({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:remediate-whats-app-number <id>',
      'Fix a declined number and re-submit',
      (y) =>
        y
          .positional('id', { type: 'string', describe: 'id', demandOption: true })
          .option('values', { type: 'string', describe: 'body param (JSON)' })
          .option('documents', { type: 'string', describe: 'body param (JSON)' })
          .option('address', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { id: argv['id'] };
          const body: Record<string, unknown> = {};
          if (argv['values'] !== undefined) body['values'] = JSON.parse(String(argv['values']));
          if (argv['documents'] !== undefined) body['documents'] = JSON.parse(String(argv['documents']));
          if (argv['address'] !== undefined) body['address'] = JSON.parse(String(argv['address']));
          const { data } = await (late as any).whatsappphonenumbers.remediateWhatsAppNumber({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:search-available-whats-app-numbers',
      'Search available numbers to purchase',
      (y) =>
        y
          .option('country', { type: 'string', describe: 'query param' })
          .option('type', { type: 'string', describe: 'query param' })
          .option('prefix', { type: 'string', describe: 'query param' })
          .option('locality', { type: 'string', describe: 'query param' })
          .option('contains', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['country'] !== undefined) query['country'] = argv['country'];
          if (argv['type'] !== undefined) query['type'] = argv['type'];
          if (argv['prefix'] !== undefined) query['prefix'] = argv['prefix'];
          if (argv['locality'] !== undefined) query['locality'] = argv['locality'];
          if (argv['contains'] !== undefined) query['contains'] = argv['contains'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          const { data } = await (late as any).whatsappphonenumbers.searchAvailableWhatsAppNumbers({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:submit-whats-app-number-kyc',
      'Submit regulated-number KYC',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true })
          .option('country', { type: 'string', describe: 'body param', demandOption: true })
          .option('submissionId', { type: 'string', describe: 'body param' })
          .option('reuse', { type: 'boolean', describe: 'body param' })
          .option('reuseFrom', { type: 'string', describe: 'body param' })
          .option('endUserFirstName', { type: 'string', describe: 'body param' })
          .option('endUserLastName', { type: 'string', describe: 'body param' })
          .option('values', { type: 'string', describe: 'body param (JSON)' })
          .option('documents', { type: 'string', describe: 'body param (JSON)' })
          .option('address', { type: 'string', describe: 'body param (JSON)' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          if (argv['country'] !== undefined) body['country'] = argv['country'];
          if (argv['submissionId'] !== undefined) body['submissionId'] = argv['submissionId'];
          if (argv['reuse'] !== undefined) body['reuse'] = argv['reuse'];
          if (argv['reuseFrom'] !== undefined) body['reuseFrom'] = argv['reuseFrom'];
          if (argv['endUserFirstName'] !== undefined) body['endUserFirstName'] = argv['endUserFirstName'];
          if (argv['endUserLastName'] !== undefined) body['endUserLastName'] = argv['endUserLastName'];
          if (argv['values'] !== undefined) body['values'] = JSON.parse(String(argv['values']));
          if (argv['documents'] !== undefined) body['documents'] = JSON.parse(String(argv['documents']));
          if (argv['address'] !== undefined) body['address'] = JSON.parse(String(argv['address']));
          const { data } = await (late as any).whatsappphonenumbers.submitWhatsAppNumberKyc({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:upload-whats-app-number-kyc-document',
      'Upload a single regulated-number KYC document',
      (y) => y,
      async (argv) => {
        try {
          const late = createClient();

          const { data } = await (late as any).whatsappphonenumbers.uploadWhatsAppNumberKycDocument();
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappphonenumbers:validate-whats-app-number-kyc-address',
      'Pre-validate a regulated-number KYC address (Tier 4)',
      (y) =>
        y
          .option('country', { type: 'string', describe: 'body param', demandOption: true })
          .option('street_address', { type: 'string', describe: 'body param', demandOption: true })
          .option('locality', { type: 'string', describe: 'body param', demandOption: true })
          .option('administrative_area', { type: 'string', describe: 'body param' })
          .option('postal_code', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['country'] !== undefined) body['country'] = argv['country'];
          if (argv['street_address'] !== undefined) body['street_address'] = argv['street_address'];
          if (argv['locality'] !== undefined) body['locality'] = argv['locality'];
          if (argv['administrative_area'] !== undefined) body['administrative_area'] = argv['administrative_area'];
          if (argv['postal_code'] !== undefined) body['postal_code'] = argv['postal_code'];
          const { data } = await (late as any).whatsappphonenumbers.validateWhatsAppNumberKycAddress({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappsandbox:create-whats-app-sandbox-session',
      'Start a sandbox activation for a phone',
      (y) =>
        y
          .option('phone', { type: 'string', describe: 'body param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['phone'] !== undefined) body['phone'] = argv['phone'];
          const { data } = await (late as any).whatsappsandbox.createWhatsAppSandboxSession({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappsandbox:delete-whats-app-sandbox-session <sessionId>',
      'Revoke a sandbox session',
      (y) =>
        y
          .positional('sessionId', { type: 'string', describe: 'sessionId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { sessionId: argv['sessionId'] };
          const { data } = await (late as any).whatsappsandbox.deleteWhatsAppSandboxSession({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsappsandbox:list-whats-app-sandbox-sessions',
      'List your sandbox sessions',
      (y) => y,
      async (argv) => {
        try {
          const late = createClient();

          const { data } = await (late as any).whatsappsandbox.listWhatsAppSandboxSessions();
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'whatsapptemplates:get-whats-app-library-template',
      'Look up a library template',
      (y) =>
        y
          .option('accountId', { type: 'string', describe: 'query param', demandOption: true })
          .option('name', { type: 'string', describe: 'query param', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['accountId'] !== undefined) query['accountId'] = argv['accountId'];
          if (argv['name'] !== undefined) query['name'] = argv['name'];
          const { data } = await (late as any).whatsapptemplates.getWhatsAppLibraryTemplate({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:activate <workflowId>',
      'Activate workflow',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const { data } = await (late as any).workflows.activateWorkflow({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:create',
      'Create workflow',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'body param', demandOption: true })
          .option('accountId', { type: 'string', describe: 'body param', demandOption: true })
          .option('platform', { type: 'string', describe: 'body param' })
          .option('name', { type: 'string', describe: 'body param', demandOption: true })
          .option('description', { type: 'string', describe: 'body param' })
          .option('nodes', { type: 'string', describe: 'body param (JSON)' })
          .option('edges', { type: 'string', describe: 'body param (JSON)' })
          .option('entryNodeId', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const body: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) body['profileId'] = argv['profileId'];
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          if (argv['platform'] !== undefined) body['platform'] = argv['platform'];
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['nodes'] !== undefined) body['nodes'] = JSON.parse(String(argv['nodes']));
          if (argv['edges'] !== undefined) body['edges'] = JSON.parse(String(argv['edges']));
          if (argv['entryNodeId'] !== undefined) body['entryNodeId'] = argv['entryNodeId'];
          const { data } = await (late as any).workflows.createWorkflow({ body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:delete <workflowId>',
      'Delete workflow',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const { data } = await (late as any).workflows.deleteWorkflow({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:duplicate <workflowId>',
      'Duplicate a workflow',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const { data } = await (late as any).workflows.duplicateWorkflow({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:get <workflowId>',
      'Get workflow with graph',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const { data } = await (late as any).workflows.getWorkflow({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:get-version <workflowId> <version>',
      'Get a specific workflow version',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true })
          .positional('version', { type: 'string', describe: 'version', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'], version: argv['version'] };
          const { data } = await (late as any).workflows.getWorkflowVersion({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:list',
      'List workflows',
      (y) =>
        y
          .option('profileId', { type: 'string', describe: 'query param' })
          .option('status', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('skip', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const query: Record<string, unknown> = {};
          if (argv['profileId'] !== undefined) query['profileId'] = argv['profileId'];
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['skip'] !== undefined) query['skip'] = argv['skip'];
          const { data } = await (late as any).workflows.listWorkflows({ query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:list-execution-events <workflowId> <executionId>',
      'Get an execution\'s timeline',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true })
          .positional('executionId', { type: 'string', describe: 'executionId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'], executionId: argv['executionId'] };
          const { data } = await (late as any).workflows.listWorkflowExecutionEvents({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:list-executions <workflowId>',
      'List workflow runs',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true })
          .option('status', { type: 'string', describe: 'query param' })
          .option('limit', { type: 'number', describe: 'query param' })
          .option('skip', { type: 'number', describe: 'query param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const query: Record<string, unknown> = {};
          if (argv['status'] !== undefined) query['status'] = argv['status'];
          if (argv['limit'] !== undefined) query['limit'] = argv['limit'];
          if (argv['skip'] !== undefined) query['skip'] = argv['skip'];
          const { data } = await (late as any).workflows.listWorkflowExecutions({ path, query: query as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:list-versions <workflowId>',
      'List a workflow\'s version history',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const { data } = await (late as any).workflows.listWorkflowVersions({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:pause <workflowId>',
      'Pause workflow',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const { data } = await (late as any).workflows.pauseWorkflow({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:restore-version <workflowId> <version>',
      'Restore a previous workflow version',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true })
          .positional('version', { type: 'string', describe: 'version', demandOption: true }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'], version: argv['version'] };
          const { data } = await (late as any).workflows.restoreWorkflowVersion({ path });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:trigger <workflowId>',
      'Manually start a workflow run',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true })
          .option('to', { type: 'string', describe: 'body param' })
          .option('conversationId', { type: 'string', describe: 'body param' })
          .option('text', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const body: Record<string, unknown> = {};
          if (argv['to'] !== undefined) body['to'] = argv['to'];
          if (argv['conversationId'] !== undefined) body['conversationId'] = argv['conversationId'];
          if (argv['text'] !== undefined) body['text'] = argv['text'];
          const { data } = await (late as any).workflows.triggerWorkflow({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    )
    .command(
      'workflows:update <workflowId>',
      'Update workflow',
      (y) =>
        y
          .positional('workflowId', { type: 'string', describe: 'workflowId', demandOption: true })
          .option('name', { type: 'string', describe: 'body param' })
          .option('description', { type: 'string', describe: 'body param' })
          .option('nodes', { type: 'string', describe: 'body param (JSON)' })
          .option('edges', { type: 'string', describe: 'body param (JSON)' })
          .option('entryNodeId', { type: 'string', describe: 'body param' })
          .option('accountId', { type: 'string', describe: 'body param' }),
      async (argv) => {
        try {
          const late = createClient();
          const path = { workflowId: argv['workflowId'] };
          const body: Record<string, unknown> = {};
          if (argv['name'] !== undefined) body['name'] = argv['name'];
          if (argv['description'] !== undefined) body['description'] = argv['description'];
          if (argv['nodes'] !== undefined) body['nodes'] = JSON.parse(String(argv['nodes']));
          if (argv['edges'] !== undefined) body['edges'] = JSON.parse(String(argv['edges']));
          if (argv['entryNodeId'] !== undefined) body['entryNodeId'] = argv['entryNodeId'];
          if (argv['accountId'] !== undefined) body['accountId'] = argv['accountId'];
          const { data } = await (late as any).workflows.updateWorkflow({ path, body: body as any });
          output(data, argv.pretty as boolean);
        } catch (err) {
          handleError(err);
        }
      },
    );
}
